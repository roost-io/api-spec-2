# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_patch for http method type PATCH in artillery framework
# RoostTestHash=1f79c3935d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "topics_topic_name_patch_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
  plugins:
    expect: {}

scenarios:
  - name: "Patch Topics scenario"
    flow:
      - log: "Patch request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
      - patch:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
            - hasProperty: "message"
        log: "Response received"
      - think: 1
