# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_patch for http method type PATCH in artillery framework

# RoostTestHash=9d07b67b96


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "topics_topic_name_patch_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "partitions_count"
    skipHeader: true

scenarios:
  - name: "Update Partition Count Scenario"
    flow:
      - log: "Updating partition count for a topic"
      - patch:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            partitions_count: "{{ partitions_count }}"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "cluster_id"
            - hasProperty: "topic_name"
            - hasProperty: "is_internal"
            - hasProperty: "replication_factor"
            - hasProperty: "partitions_count"
            - hasProperty: "partitions"
            - hasProperty: "configs"
            - hasProperty: "partition_reassignments"
            - hasProperty: "authorized_operations"
          comment: "Expecting a 200 status code with the updated topic information"
      - think: 1
