# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_post_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test scenario for non-successful API responses"
    flow:
      # Invalid payload (missing required 'name' field)
      - post:
          url: "/products"
          json:
            id: "{{ id }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 405
          # Log statement for behavior
          log: "Attempting to create a product without the required 'name' field."
      - think: 1

      # Invalid authentication (missing or incorrect auth token)
      - post:
          url: "/products"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
          # Log statement for behavior
          log: "Attempting to create a product with invalid authentication."
      - think: 1

      # Invalid URL
      - post:
          url: "/invalid-url"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404
          # Log statement for behavior
          log: "Attempting to create a product with an invalid URL."
      - think: 1

      # Invalid Content-Type header
      - post:
          url: "/products"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Content-Type: "text/plain"
          expect:
            - statusCode: 415
          # Log statement for behavior
          log: "Attempting to create a product with an invalid Content-Type header."
      - think: 1

      # Invalid price type (string instead of integer)
      - post:
          url: "/products"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "invalid-price"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 405
          # Log statement for behavior
          log: "Attempting to create a product with a string value for price."
      - think: 1
