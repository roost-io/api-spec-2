# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /GetRTSPURL_get for http method type GET in artillery framework
# RoostTestHash=3f25b96b7c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetRTSPURL_get_success.csv"
    fields:
      - "jetson_uuid"
      - "cameraId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "GetRTSPURL API Test"
    flow:
      - log: "Sending GET request to /GetRTSPURL endpoint with valid jetson_uuid and cameraId"
      - get:
          url: "/GetRTSPURL?params={{ jetson_uuid }}&cameraId={{ cameraId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
            - hasProperty: "cameraId"
      - think: 1
      - log: "Sending GET request to /GetRTSPURL endpoint with invalid jetson_uuid and cameraId"
      - get:
          url: "/GetRTSPURL?params=invalid&cameraId=invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending GET request to /GetRTSPURL endpoint with missing parameters"
      - get:
          url: "/GetRTSPURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
