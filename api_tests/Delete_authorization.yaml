# Test generated by RoostGPT for test credwallet-integration-artillery-updated using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10
  plugins:
    expect: {}
  payload:
    path: "Delete_authorization.csv"
    fields:
      - "ID"
    skipHeader: true

scenarios:
  - name: "Delete authorization"
    flow:
      - delete:
          url: "/authorizations/{{ ID }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
          log: "Delete request sent to /authorizations/{{ ID }}"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - notHasProperty: "{{ ID }}"
          log: "GET request sent to /authorizations to verify deletion"
      - think: 1
