# Test generated by RoostGPT for test credwallet-integration-artillery-updated using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "Delete_authorization.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      auth_token: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Delete authorization"
    flow:
      - log: "Delete authorization"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "headers"
            - hasProperty: "headers.api-version"
            - equals: 
                headers.api-version: "1.0.0"
            - matchesRegexp: 
                body: "^{}$" # Expecting an empty object as a response body
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            Token: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "body"
            - notHasProperty: "body[?(@.id=='{{ id }}')]" # Assert that deleted authorization is not in the list
      - think: 1
