# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  environments:
    staging:
      target: "http://localhost:8080"
      phases:
        - duration: 60
          arrivalRate: 20
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Delete authorization"
    flow:
      - log: "Given authorization ID"
      - delete:
          url: "/authorizations/{{ authorizationID }}"
          headers:
            authorization: "Bearer {{ AUTH_TOKEN }}"
          expect:
            - statusCode: 200
      - think: 1
      - log: "the response status code should be 200 for a successful delete"
      - get:
          url: "/authorizations"
          headers:
            authorization: "Bearer {{ AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "authorizations"
            - equals: 
                authorizations: []
      - think: 1
      - log: "the authorizations should not appear in GET request for '/authorizations'"
