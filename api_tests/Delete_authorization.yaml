# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "Delete_authorization.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      phases:
        - duration: 60
          arrivalRate: 5
          name: "Warm up the application"
        - duration: 120
          arrivalRate: 10
          name: "Sustained load"
scenarios:
  - name: "Delete authorization"
    flow:
      - log: "Starting scenario: Delete authorization"
      - delete:  # Changed from post to delete as per scenario
          url: "/authorizations/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200  # Expecting 200 as per scenario
          think: 1
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - not:  # Verify authorization ID should not appear
                hasProperty: "{{ id }}"
          think: 1
      - log: "Ending scenario: Delete authorization"
