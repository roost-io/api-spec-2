# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "Delete_authorization.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Delete authorization"
    flow:
      # Scenario: Delete an authorization
      - delete:  # Changed from post to delete as per the scenario
          url: "/authorizations/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200  # Expected status code after successful deletion
          think: 1

      # Scenario: Verify the authorization is deleted successfully
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - not: 
                equals: 
                  - "{{ id }}"
                  - "{{ response.body.id }}"  # The deleted id should not be present in the authorizations list
          think: 1
