# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields: ["houseId"]
    skipHeader: true

scenarios:
  - name: "Device List Scenario"
    flow:
      - log: "Sending GET request to /device/list endpoint"
      - get:
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          url: "/device/list?houseId={{ houseId }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
          afterResponse: "logResponse"
      - think: 1
      - log: "Sending GET request to /device/list endpoint with different houseId"
      - get:
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          url: "/device/list?houseId={{ houseId }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
          afterResponse: "logResponse"
      - think: 1

    afterScenario:
    - log: "Test scenario completed"
