# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "houseId"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Successful device list retrieval"
    flow:
      - log: "Sending request to get device list"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
            - body: 
                matchesJSONSchema:
                  type: "array"
                  items:
                    type: "object"
                    properties:
                      jetson_uuid:
                        type: "string"
      - think: 1
