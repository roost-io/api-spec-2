# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /device/camera/list_get for http method type GET in artillery framework

# RoostTestHash=202607d963


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    - fields:
        - jetson_uuid
      src: "list_get_success.csv"
      skipHeader: true

scenarios:
  - name: Camera list API test scenario
    flow:
      - log: "Running camera list API test scenario"
      - get:
          url: "/device/camera/list?params={{ jetson_uuid }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - status: 200
            - hasProperty: 'no'
            - hasProperty: 'id'
            - hasProperty: 'status'
      - think: 3
      - log: "Finished camera list API test scenario"
