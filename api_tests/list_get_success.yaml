# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /device/list_get for http method type GET in artillery framework
# RoostTestHash=c1f7bc6396

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "houseId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Get the device list that registered with the house"
    flow:
      - log: "Sending a GET request to /device/list with valid houseId"
      - get:
          url: "/device/list"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            params:
              houseId: "{{ houseId }}"
          expect:
            - statusCode: 200
            - hasProperty: "content"
            - hasHeader: "content-type"
      - think: 1
