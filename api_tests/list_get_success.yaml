# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /device/camera/list_get for http method type GET in artillery framework

# RoostTestHash=202607d963


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "jetson_uuid"
    skipHeader: true
  environments:
    development:
      target: "http://localhost:3000"

scenarios:
  - name: 'Successful API requests'
    flow:
      # Get the camera list from the device
      - get:
          url: "/device/camera/list?jetson_uuid={{ jetson_uuid }}"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.length"
            as: "camera_count"
          expect:
            - statusCode: 200
            - hasProperty: "no"
            - hasProperty: "id"
            - hasProperty: "status"
          # Think time after "get" request
          think: 1
      # Log the number of cameras fetched from each 'get' request
      - log: "Fetched {{ camera_count }} cameras"
