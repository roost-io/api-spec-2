# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "houseId"
    skipHeader: true
  environments:
    staging:
      target: "http://staging.api.myapp.com"
    production:
      target: "http://api.myapp.com"

scenarios:
  - name: "Get device list that registered with the house"
    flow:
      - get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
            - bodySchema:
                type: "array"
                items:
                  type: "object"
                  properties:
                    jetson_uuid:
                      type: "string"
          afterResponse: "logResponse"

processors:
  logResponse:
    function(requestParams, response, context, ee, next) {
      console.log('Response:', response);
      return next(); 
    }
