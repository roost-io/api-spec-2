# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "houseId"
    skipHeader: true
  headers:
    Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Device list API test"
    flow:
      - log: "Requesting device list"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
            - hasProperty: "status"
            - propertyType: ["jetson_uuid", "String"]
            - propertyType: ["status", "String"]
        think: 1

      - log: "Requesting device list with invalid houseId"
        get:
          url: "/device/list?houseId=invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
