# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


---
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    default:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}  
  payload:
    path: "list_get_success.csv"
    fields: 
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Device List API calls scenario"
    flow:
      # Request to get list of devices
      - get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            # Verifying if the response status is 200
            - statusCode: 200
            # Checking if 'jetson_uuid' property is present in the response body
            - hasProperty: "jetson_uuid"
            # Checking the data type of 'jetson_uuid'
            - equals:
                - "{{ typeof jetson_uuid }}"
                - "string"
          afterResponse: "logRelevantInfo"

      # adding delay before sending next request
      - think: 1

functions:
  logRelevantInfo:
    - log: "Successfully retrieved the list of devices"
