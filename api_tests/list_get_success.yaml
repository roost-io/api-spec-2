# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /device/list_get for http method type GET in artillery framework
# RoostTestHash=c1f7bc6396

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "list_get_success.csv"
    fields:
      - "houseId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Get Device List Test Scenario"
    flow:
      - log: "Get request to fetch the list of devices registered with the house"
      - get:
          url: "/device/list?params={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "deviceList"
          think: 1

      - log: "Another get request to fetch the list of devices registered with the house"
      - get:
          url: "/device/list?params={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "deviceList"
          think: 1
