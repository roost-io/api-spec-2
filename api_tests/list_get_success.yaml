# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    skipHeader: true
  environments:
    dev:
      target: "http://localhost:3000"

scenarios:
  - name: "API Success requests"
    flow:
      - log: "Request to get device list"
        get:
          url: "/device/list?params={{ field1 }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
            
      - think: 5
      
      - log: "Device list retrieved successfully"
        get:
          url: "/device/list?params={{ field2 }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
