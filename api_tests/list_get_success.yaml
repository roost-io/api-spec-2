# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /device/camera/list_get for http method type GET in artillery framework

# RoostTestHash=202607d963


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "jetson_uuid"
    skipHeader: true

scenarios:
  - name: "API request scenario"
    flow:
      - log: "Requesting: list of cameras"
        get: 
          url: "/device/camera/list?params={{ jetson_uuid }}"
          # Refer to the API spec for headers
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "no"
            - hasProperty: "id"
            - hasProperty: "status"
            - hasHeader: "content-type"
        think: 1

      - log: "Validating camera list response"
        get: 
          url: "/device/camera/list?params={{ jetson_uuid }}"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "no"
            - hasProperty: "id"
            - hasProperty: "status"
            - hasHeader: "content-type"
            - equals:
                no: "integer"
                id: "string"
                status: "string"
        think: 1
