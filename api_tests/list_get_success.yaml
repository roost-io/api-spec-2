# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/list_get_success.csv"
    fields:
      - "houseId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Get device list for a house"
    flow:
      - get:
          url: "/device/list?houseId={{ houseId }}"
          # Expected behavior: Retrieve a list of devices registered with the given house ID.
          log: "Fetching device list for houseId {{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
            - hasHeader: "content-type"
          think: 2

