# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /device/list_get for http method type GET in artillery framework
# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields: ["houseId"]
    skipHeader: true
scenarios:
  - name: "Get the device list that registered with the house"
    flow:
      - log: "Fetching the device list"
      - get:
          url: "/device/list?params={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
            - hasHeader: "content-type"
            - bodyContains:
                - "9a48824e-829d-11ee-b962-0242ac120002"
                - "bb791d8e-829d-11ee-b962-0242ac120002"
                - "c79794c4-829d-11ee-b962-0242ac120002"
          # The API should return a list of devices registered with the house
      - think: 1
