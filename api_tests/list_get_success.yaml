# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /device/camera/list_get for http method type GET in artillery framework

# RoostTestHash=202607d963


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "jetson_uuid"
    skipHeader: true

scenarios:
  - name: "Test for successful API requests"
    flow:
      - get:
          url: "/device/camera/list?jetson_uuid={{ jetson_uuid }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            # Check for response headers
            - hasHeader: "content-type"
            - hasProperty: "no"
            - hasProperty: "id"
            - hasProperty: "status"
            # Check for response body structure
            - equals: ["{{ response.body.no }}", "integer"]
            - equals: ["{{ response.body.id }}", "string"]
            - equals: ["{{ response.body.status }}", "string"]
            # Check for response body data
            - equals: ["{{ response.body.no }}", 1]
            - equals: ["{{ response.body.id }}", "9a48824e-829d-11ee-b962-0242ac120002"]
            - equals: ["{{ response.body.status }}", "online"]
          log: "Validating the response for /device/camera/list API with valid payload"
        think: 1
