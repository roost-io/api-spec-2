# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "houseId"
    skipHeader: true
  variables:
    authToken: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test Device API"
    flow:
      - get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          capture:
            json: "$.body"
            as: "responseBody"
          expect:
            - statusCode: 200
            # check for uuid in response
            - hasProperty: 'jetson_uuid'
            # check format of uuid
            - equals: 
              - "{{ responseBody.format }}"
              - "uuid"
        log: "Sent a GET request to the /device/list endpoint with valid houseId"
        think: 5
