# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /device/list_get for http method type GET in artillery framework

# RoostTestHash=fe16559128


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "list_get_success.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Test scenario for device list API"
    flow:
      - log: "Starting test for device list API"
      - get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "jetson_uuid"
            # Asserting the format of the property jetson_uuid to UUID
            - equals: 
                - "{{ response.body.jetson_uuid }}"
                - "/\b[0-9a-f]{8}\b-[0-9a-f]{4}-[0-9a-f]{4}-[0-9a-f]{4}-\b[0-9a-f]{12}\b/"
      - think: 1
