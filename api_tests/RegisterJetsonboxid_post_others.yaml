# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /RegisterJetsonboxid_post for http method type POST in artillery framework

# RoostTestHash=c528c38714


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "RegisterJetsonboxid_post_others.csv"
    fields:
      - "jetson_uuid"
      - "username"
      - "clusterId"
      - "houseId"
    skipHeader: true
  plugins:
    expect: {}
  environments:
    staging:
      target: "http://staging-api.example.com"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Non-success API requests"
    flow:
      - log: "Sending invalid payload"
        post:
          url: "/RegisterJetsonboxid?jetson_uuid={{ jetson_uuid }}&username={{ username }}&clusterId={{ clusterId }}&houseId={{ houseId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # Expecting Bad Request due to invalid payload
            - hasProperty: "error"
            - equals: 
                - "error.message": "Invalid payload"
          think: 1
      - log: "Sending request without authentication"
        post:
          url: "/RegisterJetsonboxid?jetson_uuid={{ jetson_uuid }}&username={{ username }}&clusterId={{ clusterId }}&houseId={{ houseId }}"
          expect:
            - statusCode: 401 # Expecting Unauthorized due to missing authentication
            - hasProperty: "error"
            - equals: 
                - "error.message": "Authentication required"
          think: 1
      - log: "Sending request to incorrect URL"
        post:
          url: "/InvalidURL"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Expecting Not Found due to incorrect URL
            - hasProperty: "error"
            - equals: 
                - "error.message": "Not Found"
          think: 1
