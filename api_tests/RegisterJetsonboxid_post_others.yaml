# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /RegisterJetsonboxid_post for http method type POST in artillery framework

# RoostTestHash=c528c38714


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "RegisterJetsonboxid_post_others.csv"
    fields:
      - "jetson_uuid"
      - "username"
      - "clusterId"
      - "houseId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      - post:
          url: "/RegisterJetsonboxid?jetson_uuid={{ jetson_uuid }}&username={{ username }}&clusterId={{ clusterId }}&houseId={{ houseId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Expecting a Bad Request error due to invalid payload
            - hasProperty: "error"
            - equals: ["message", "Invalid payload"]
          think: 1

      - post:
          url: "/RegisterJetsonboxid?jetson_uuid={{ jetson_uuid }}&username={{ username }}&clusterId={{ clusterId }}&houseId={{ houseId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401 # Expecting an Unauthorized error due to invalid token
            - hasProperty: "error"
            - equals: ["message", "Invalid token"]
          think: 1

      - post:
          url: "/InvalidURL"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting a Not Found error due to invalid URL
            - hasProperty: "error"
            - equals: ["message", "Not Found"]
          think: 1
