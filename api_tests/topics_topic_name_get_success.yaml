# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_get for http method type GET in artillery framework
# RoostTestHash=4186ba69da

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "topics_topic_name_get_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "include_authorized_operations"
    skipHeader: true

scenarios:
  - name: "Test /kafka/v3/clusters/{cluster_id}/topics/{topic_name} endpoint"
    flow:
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}?include_authorized_operations={{ include_authorized_operations }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest:
            - "log": "'Making GET request with cluster_id: {{ cluster_id }}, topic_name: {{ topic_name }} and include_authorized_operations: {{ include_authorized_operations }}'"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
