# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_get for http method type GET in artillery framework

# RoostTestHash=dc56f44070


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "topics_topic_name_get_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "include_authorized_operations"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Get Topic Scenario"
    flow:
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}?include_authorized_operations={{ include_authorized_operations }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Fetching topic details with authorization"
          # Expected to successfully retrieve topic details
          think: 1
          capture:
            json: "$.metadata.self"
            as: "self_url"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "cluster_id"
            - hasProperty: "topic_name"
            - hasProperty: "is_internal"
            - hasProperty: "replication_factor"
            - hasProperty: "partitions_count"
            - hasProperty: "partitions"
            - hasProperty: "configs"
            - hasProperty: "partition_reassignments"
            - hasHeader: "Content-Type"
