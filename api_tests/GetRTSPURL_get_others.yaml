# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3f25b96b7c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "GetRTSPURL_get_others.csv"
    fields:
      - "jetson_uuid"
      - "cameraId"
    skipHeader: true

scenarios:
  - name: "Testing GetRTSPURL API"
    flow:
      - log: "Test Case 1: Sending request without required query parameters. Expecting 400 status code."
        get:
          url: "/GetRTSPURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 5

      - log: "Test Case 2: Sending request with invalid jetson_uuid. Expecting 500 status code."
        get:
          url: "/GetRTSPURL?jetson_uuid=invalid&cameraId={{ cameraId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
        think: 5

      - log: "Test Case 3: Sending request with invalid Authorization token. Expecting 401 status code."
        get:
          url: "/GetRTSPURL?jetson_uuid={{ jetson_uuid }}&cameraId={{ cameraId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
        think: 5

      - log: "Test Case 4: Sending request to invalid URL. Expecting 404 status code."
        get:
          url: "/InvalidURL?jetson_uuid={{ jetson_uuid }}&cameraId={{ cameraId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 5
