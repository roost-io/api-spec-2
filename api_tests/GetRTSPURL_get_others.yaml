# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3f25b96b7c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "GetRTSPURL_get_others.csv"
    fields:
      - "jetson_uuid"
      - "cameraId"
    skipHeader: true

scenarios:
  - name: "Testing GetRTSPURL API"
    flow:
      - log: "Test Case 1: Sending request without required query parameters. Expecting 400 Bad Request response"
        get:
          url: "/GetRTSPURL"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
        think: 2
      - log: "Test Case 2: Sending request with invalid url. Expecting 404 Not Found response"
        get:
          url: "/GetRTSPURL_invalid"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
        think: 2
      - log: "Test Case 3: Sending request with invalid authorization token. Expecting 401 Unauthorized response"
        get:
          url: "/GetRTSPURL?jetson_uuid={{ jetson_uuid }}&cameraId={{ cameraId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "message"
        think: 2
      - log: "Test Case 4: Sending request with valid parameters but server error. Expecting 500 Internal Server Error response"
        get:
          url: "/GetRTSPURL?jetson_uuid={{ jetson_uuid }}&cameraId={{ cameraId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "error"
            - hasProperty: "message"
        think: 2
