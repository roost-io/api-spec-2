# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3f25b96b7c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "GetRTSPURL_get_others.csv"
    fields:
      - "cameraId"
      - "jetson_uuid"
    skipHeader: true

scenarios:
  - name: "Testing GetRTSPURL API"
    flow:
      - log: "Test Case 1: Sending a request with invalid URL. Expecting a 404 status code"
        get:
          url: "/GetRTSPURLInvalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 5

      - log: "Test Case 2: Sending a request without required query parameters. Expecting a 400 status code"
        get:
          url: "/GetRTSPURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 5

      - log: "Test Case 3: Sending a request with invalid Authorization token. Expecting a 401 status code"
        get:
          url: "/GetRTSPURL?params={{ cameraId }}&params={{ jetson_uuid }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
        think: 5

      - log: "Test Case 4: Sending a request with valid query parameters but server error occurs. Expecting a 500 status code"
        get:
          url: "/GetRTSPURL?params={{ cameraId }}&params={{ jetson_uuid }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
        think: 5
