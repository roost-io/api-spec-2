# Test generated by RoostGPT for test nobelprize-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /nobelPrizes_get for http method type GET in artillery framework

# RoostTestHash=4940f5c55b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "nobelPrizes_get_success.csv"
    fields:
      - "offset"
      - "limit"
      - "sort"
      - "nobelPrizeYear"
      - "yearTo"
      - "nobelPrizeCategory"
      - "format"
      - "csvLang"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Validate correct API behavior with valid input payloads"
    flow:
      - log: "Sending request to obtain Nobel Prizes information with valid parameters"
      - get:
          url: "/2.1/nobelPrizes?offset={{ offset }}&limit={{ limit }}&sort={{ sort }}&nobelPrizeYear={{ nobelPrizeYear }}&yearTo={{ yearTo }}&nobelPrizeCategory={{ nobelPrizeCategory }}&format={{ format }}&csvLang={{ csvLang }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            # Add assertions for each required property in the response body against the provided schema
            - hasProperty: "nobelPrizes"
            - hasProperty: "meta"
            - hasProperty: "links"
      - think: 1

      - log: "Sending request to obtain Nobel Prizes information for a specific year"
      - get:
          url: "/2.1/nobelPrizes?nobelPrizeYear={{ nobelPrizeYear }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "nobelPrizes"
      - think: 1

      - log: "Sending request to obtain Nobel Prizes information sorted in ascending order"
      - get:
          url: "/2.1/nobelPrizes?sort=asc"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "nobelPrizes"
      - think: 1

      - log: "Sending request to obtain Nobel Prizes information in CSV format"
      - get:
          url: "/2.1/nobelPrizes?format=csv&csvLang=en"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            # Expectation for CSV format can be added if necessary
      - think: 1
