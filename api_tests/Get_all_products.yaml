# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "Get_all_products.csv"
    fields:
      - "productId"
    skipHeader: true
  headers:
    Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Get all products"
    flow:
      - log: "Sending GET request to /products to get the list of all products"
      - get:
          url: "/products"
          expect:
            - statusCode: 200
            - hasProperty: "products"
      - think: 1
      - log: "The list of products returned should be empty"
      - get:
          url: "/products"
          expect:
            - statusCode: 200
            - equals:
                jsonpath: "$.length"
                value: 0
      - think: 1
