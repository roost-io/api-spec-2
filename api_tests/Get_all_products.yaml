# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "Get_all_products.csv"
    fields:
      - "productId"
    skipHeader: true
  headers:
    Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Get all products"
    flow:
      - get:
          url: "/products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200 
            - hasProperty: "products" 
            - equals:
                products: [] 
          # The response from the server should have a status code 200
          # The response should have a property named 'products'
          # The 'products' property should be an empty array
          think: 1
