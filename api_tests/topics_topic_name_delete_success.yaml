# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_delete for http method type DELETE in artillery framework
# RoostTestHash=5f8e80468a

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "topics_topic_name_delete_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
    skipHeader: true

scenarios:
  - name: "Delete a topic"
    flow:
      - log: "Starting test for delete topic API"
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 204
          afterResponse: "logResponse"
      - think: 1
      - log: "Test for delete topic API finished"
