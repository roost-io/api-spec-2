# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_delete for http method type DELETE in artillery framework

# RoostTestHash=b9526a5d59


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "topics_topic_name_delete_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
    skipHeader: true

scenarios:
  - name: "Delete Kafka Topic Scenario"
    flow:
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Attempting to delete Kafka topic with valid credentials and existing topic name."
          # Expecting a successful deletion with no content returned
          expect:
            - statusCode: 204
          think: 1

      # More requests can be added here with their respective log, think, and expect blocks
