# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "Create_a_new_product.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Create a new product"
    flow:
      - post:
          url: "/products"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 201
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - equals:
                - "{{ name }}"
                - "{{ response.body.name }}"
            - equals:
                - "{{ description }}"
                - "{{ response.body.description }}"
            - equals:
                - "{{ price }}"
                - "{{ response.body.price }}"
          think: 1

      - log: "Created a new product"

      - get:
          url: "/products/{{ response.body.id }}"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - equals:
                - "{{ name }}"
                - "{{ response.body.name }}"
            - equals:
                - "{{ description }}"
                - "{{ response.body.description }}"
            - equals:
                - "{{ price }}"
                - "{{ response.body.price }}"
          think: 1

      - log: "Verified the created product"
