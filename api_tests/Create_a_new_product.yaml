# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "Create_a_new_product.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Create a new product"
    flow:
      - post:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 201  # Expecting a 201 status code for successful creation
            - hasProperty: "name"  # Checking if the response has the 'name' property
            - equals:  # Verifying if the response properties match the provided values
                name: "{{ name }}"
                description: "{{ description }}"
                price: "{{ price }}"
      - think: 1
