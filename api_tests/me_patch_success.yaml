# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_patch_success.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Change my settings or account name"
    flow:
      - log: "Updating account name"
      - put:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
          expect:
            - statusCode: 200
            - hasProperty: 'id'
            - hasHeader: 'api-version'
      - think: 1

      - log: "Updating account image"
      - put:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            image: "{{ image }}"
          expect:
            - statusCode: 200
            - hasProperty: 'image'
            - hasHeader: 'api-version'
      - think: 1

      - log: "Updating account settings"
      - put:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            settings: "{{ settings }}"
          expect:
            - statusCode: 200
            - hasProperty: 'settings'
            - hasHeader: 'api-version'
      - think: 1
