# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_patch_success.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.example.com"
    production:
      target: "https://api.example.com"

scenarios:
  - name: "Validate correct API behavior with valid input payloads"
    flow:
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - matchesJsonPath: "$.id"
            - matchesJsonPath: "$.did"
            - matchesJsonPath: "$.name"
            - matchesJsonPath: "$.image"
            - matchesJsonPath: "$.email"
            - matchesJsonPath: "$.settings"
            - matchesJsonPath: "$.createdAt"
            - matchesJsonPath: "$.updatedAt"
          log: "Sending valid payload to change my settings or account name"
      - think: 1 # Think time after the first request

      # Add additional requests here following the same pattern
      # Each request should have its own log, think time, and assertions as shown above
