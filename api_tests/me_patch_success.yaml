# Test generated by RoostGPT for test credential-artillery-test-withlogs using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_patch_success.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Validate API behavior with valid inputs"
    flow:
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
          log: "Changing account settings or name"
          think: 1

      - log: "Account settings or name change attempted"

      - get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
          log: "Retrieving account information"
          think: 1

      - log: "Account information retrieved"

      - put:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 200
            - hasProperty: "updatedAt"
          log: "Updating account information"
          think: 1

      - log: "Account information updated"
