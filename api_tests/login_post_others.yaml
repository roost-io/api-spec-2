# ********RoostGPT********
# # Test generated by RoostGPT for test qlue-single-api-test using AI Type Open AI and AI Model gpt-4

# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "login_post_others.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true

scenarios:
  - name: "Test non-success API responses"
    flow:
      # Test with invalid email
      - post:
          url: "/login"
          json:
            email: "invalid_email"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["error", "Invalid email format"]
          think: 1

      # Test with missing OTP
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            client: "{{ client }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["error", "Missing OTP"]
          think: 1

      # Test with invalid URL
      - post:
          url: "/invalid_url"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1
