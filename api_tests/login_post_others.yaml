# ********RoostGPT********
# Test generated by RoostGPT for test qlue-single-api-test using AI Type Open AI and AI Model gpt-4

# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "login_post_others.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Testing non-success API responses"
    flow:
      # Invalid email format
      - post:
          url: "/login"
          json:
            email: "invalidEmail"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["error", "Invalid email format"]
          think: 1

      # Invalid OTP
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: "wrongOTP"
            client: "{{ client }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["error", "Invalid OTP"]
          think: 1

      # Missing client
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["error", "Missing client"]
          think: 1

      # Invalid URL
      - post:
          url: "/logn"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1
