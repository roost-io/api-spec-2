# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /GetStatus_get for http method type GET in artillery framework

# RoostTestHash=637351748e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  processor: "./processors.js"
  environments:
    production:
      target: "https://production.example.com"
    staging:
      target: "https://staging.example.com"
  payload:
    path: "GetStatus_get_success.csv"
    skipHeader: true
    fields:
      - "JetsonID"
  plugins:
    expect: {}

scenarios:
  - name: "Validate correct API behavior with valid input payloads"
    flow:
      - get:
          url: "/GetStatus?JetsonID={{ JetsonID }}"
          headers: 
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "sn"
            - hasProperty: "firmware"
            - hasProperty: "regStatus"
            - hasProperty: "lastOnline"
            - hasProperty: "noCamera"
            - hasProperty: "Cameras"
            - matchesRegexp:
                sn: "^[0-9A-Z]{11}$"
            - matchesRegexp:
                firmware: "^([0-9]+\\.){2}[0-9]+$"
            - matchesRegexp:
                regStatus: "^(Registered|Unregistered)$"
            - matchesRegexp:
                lastOnline: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$"
            - equals:
                noCamera: 3
            - contentType: "application/json"
        log: "GET Request to retrieve device status"
      - think: 1

