# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /GetStatus_get for http method type GET in artillery framework

# RoostTestHash=637351748e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetStatus_get_success.csv"
    fields:
      - "JetsonID"
    skipHeader: true
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Get Status of Device"
    flow:
      - get:
          url: "/GetStatus?JetsonID={{ JetsonID }}"
          expect:
            - statusCode: 200
            # Asserting the presence of response headers
            - hasHeader: "content-type"
            # Asserting the required properties in the response body
            - hasProperty: "sn"
            - hasProperty: "firmware"
            - hasProperty: "regStatus"
            - hasProperty: "lastOnline"
            - hasProperty: "noCamera"
            - hasProperty: "Cameras"
            # Asserting the data type of properties
            - equals: 
                - "{{ $response.body.sn }}"
                - "string"
                - "{{ $response.body.firmware }}"
                - "string"
                - "{{ $response.body.regStatus }}"
                - "string"
                - "{{ $response.body.lastOnline }}"
                - "string"
                - "{{ $response.body.noCamera }}"
                - "integer"
                - "{{ $response.body.Cameras }}"
                - "array"
          # Log statement for each request
          afterResponse: "log"
      # Think time after each request
      - think: 1
