# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /GetStatus_get for http method type GET in artillery framework

# RoostTestHash=637351748e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_success.csv"
    fields: ["JetsonID"]
    skipHeader: true
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Get the status of device Jetson Nano including the Cameras"
    flow:
      - log: "Starting test scenario for GetStatus endpoint"
      - get:
          url: "/GetStatus?JetsonID={{ JetsonID }}"
          expect:
            - statusCode: 200
            - hasProperty: "sn"
            - hasProperty: "firmware"
            - hasProperty: "regStatus"
            - hasProperty: "lastOnline"
            - hasProperty: "noCamera"
            - hasProperty: "cameras"
          # Check the array items properties
            - hasProperty: "cameras.0.no"
            - hasProperty: "cameras.0.id"
            - hasProperty: "cameras.0.status"
            - hasProperty: "cameras.0.lastOnline"
      - think: 1
      - log: "GetStatus request completed successfully"
