# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /products/{productId}_put for http method type PUT in artillery framework

# RoostTestHash=7b2a6b6959


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_productId_put_success.csv"
    fields:
      - "productId"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - flow:
      - put:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - matchesRegexp: { key: "id", regexp: "\\d+" }
            - matchesRegexp: { key: "name", regexp: "[a-zA-Z]+" }
            - matchesRegexp: { key: "description", regexp: ".*" }
            - matchesRegexp: { key: "price", regexp: "\\d+" }
          log: "Updating product with ID {{ productId }}"
      - think: 1
