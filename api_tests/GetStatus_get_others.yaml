# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /GetStatus_get for http method type GET in artillery framework

# RoostTestHash=637351748e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_others.csv"
    fields:
      - "JetsonID"
    skipHeader: true
  environments:
    dev:
      target: "https://dev.example.com"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Invalid URL"
        get:
          url: "/InvalidURL"
          expect:
            - statusCode: 404
        think: 2

      - log: "Invalid Payload"
        post:
          url: "/GetStatus"
          headers:
            {{ SECURITY_SCHEMA.name }}: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            JetsonID: "InvalidJetsonID"
          expect:
            - statusCode: 400
        think: 2

      - log: "Missing Authorization Header"
        get:
          url: "/GetStatus?JetsonID={{ JetsonID }}"
          expect:
            - statusCode: 401
        think: 2

      - log: "Invalid Authorization Token"
        get:
          url: "/GetStatus?JetsonID={{ JetsonID }}"
          headers:
            {{ SECURITY_SCHEMA.name }}: "InvalidToken"
          expect:
            - statusCode: 403
        think: 2

      - log: "Valid Request"
        get:
          url: "/GetStatus?JetsonID={{ JetsonID }}"
          headers:
            {{ SECURITY_SCHEMA.name }}: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
        think: 2
