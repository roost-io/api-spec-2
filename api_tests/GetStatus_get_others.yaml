# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /GetStatus_get for http method type GET in artillery framework

# RoostTestHash=637351748e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_others.csv"
    fields:
      - "JetsonID"
    skipHeader: true

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending GET request with valid JetsonID"
        get:
          url: "/GetStatus?JetsonID={{ JetsonID }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400 # Expecting Bad Request
        think: 1

      - log: "Sending GET request with missing JetsonID"
        get:
          url: "/GetStatus"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 500 # Expecting Parameter Error
        think: 1

      - log: "Sending GET request to invalid URL"
        get:
          url: "/InvalidURL"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404 # Expecting Not Found
        think: 1
