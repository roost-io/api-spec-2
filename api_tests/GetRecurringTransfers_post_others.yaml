# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=7e03af5a84

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payloads:
    - path: "GetRecurringTransfers_post_others.csv"
      fields:
        - "customerId"
        - "status"
      skipHeader: true

scenarios:
  - name: "Non-success API Calls"
    flow:
      - log: "Testing with missing 'botId' header..."
        post: 
          url: "/GetRecurringTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            expect:
              - statusCode: 401
              - hasProperty: "err.message"
            # Expecting a HTTP 401 status code because 'botId' is required

      - think: 5

      - log: "Testing with incorrect endpoint..."
        post: 
          url: "/GetRecurringTransfers1"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
            # Expecting a HTTP 404 status code because endpoint does not exist

      - think: 5

      - log: "Testing with invalid Authorization token..."
        post: 
          url: "/GetRecurringTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
            expect:
              - statusCode: 401
              - hasProperty: "err.message"
            # Expecting a HTTP 401 status code because Authorization token is invalid 

      - think: 5

      - log: "Testing with incorrect customer id in payload..."
        post: 
          url: "/GetRecurringTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "wrongCustomerId"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
            # Expecting a HTTP 404 because of invalid customerId
