# ********RoostGPT********
#
Test generated by RoostGPT for test qlue-single-api-test using AI Type Open AI and AI Model gpt-4

Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework
RoostTestHash=49b35fa9c1


#

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_get_success.csv"
    fields:
      - "id"
      - "credentialUrl"
      - "size"
      - "offset"
  plugins:
    expect: {}
  defaults:
    headers:
      Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "GET /recipients/branches/{id}/registrations"
    flow:
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credentialUrl }}&size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 200
            - hasProperty: "headers.api-version"
            - hasProperty: "headers.page-size"
            - hasProperty: "headers.page-offset"
            - hasProperty: "body.total"
            - hasProperty: "body.registrations"
            - hasProperty: "body.registrations[0].id"
            - hasProperty: "body.registrations[0].reference"
            - hasProperty: "body.registrations[0].startDate"
            - hasProperty: "body.registrations[0].endDate"
            - hasProperty: "body.registrations[0].status"
            - hasProperty: "body.registrations[0].credentialUrl"
            - hasProperty: "body.registrations[0].suspendedUntil"
            - hasProperty: "body.registrations[0].issuedAt"
            - hasProperty: "body.registrations[0].createdAt"
            - hasProperty: "body.registrations[0].updatedAt"
          afterResponse: "think"
