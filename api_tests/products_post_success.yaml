# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /products_post for http method type POST in artillery framework

# RoostTestHash=dcd186097b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "products_post_success.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - flow:
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          log: "Creating a new product with valid input."
          # Expected outcome: Product is created with the provided details.
          think: 1
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - matchesRegexp:
                property: "name"
                pattern: "^[a-zA-Z0-9 ]+$"
            - matchesRegexp:
                property: "description"
                pattern: "^[a-zA-Z0-9 ]+$"
            - contentType: "application/json"
