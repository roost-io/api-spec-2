# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=7b2a6b6959

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_put_others.csv"
    fields:
      - "productId"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  environments:
    development:
      API_HOST: "http://dev.api.example.com"
    production:
      API_HOST: "https://api.example.com"

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      # Invalid Product ID
      - put:
          url: "/products/{{ productId }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          log: "Testing update with invalid product ID, expecting 404 response."
      - think: 1

      # Missing required field 'name' in payload
      - put:
          url: "/products/{{ productId }}"
          json:
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          log: "Testing update with missing 'name', expecting 400 response."
      - think: 1

      # Invalid Authentication Token
      - put:
          url: "/products/{{ productId }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          log: "Testing update with invalid auth token, expecting 401 response."
      - think: 1

      # Invalid URL
      - put:
          url: "/invalidEndpoint/{{ productId }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          log: "Testing update with invalid URL, expecting 404 response."
      - think: 1

      # Invalid Content-Type
      - put:
          url: "/products/{{ productId }}"
          body: "name={{ name }}&description={{ description }}&price={{ price }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/x-www-form-urlencoded"
          expect:
            - statusCode: 415
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          log: "Testing update with invalid Content-Type, expecting 415 response."
      - think: 1
