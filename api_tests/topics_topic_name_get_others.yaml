# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=dc56f44070

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "topics_topic_name_get_others.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "include_authorized_operations"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.api.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Get Topic - Error Handling Scenario"
    flow:
      - log: "Sending request with invalid authorization header to test authentication failure"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 2

      - log: "Sending request with invalid cluster_id to test not found error"
      - get:
          url: "/kafka/v3/clusters/invalid-cluster-id/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 2

      - log: "Sending request with invalid query parameter value to test bad request error"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}?include_authorized_operations=notabool"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 2

      - log: "Sending request to test rate limiting error (429)"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
      - think: 2

      - log: "Sending request with missing topic_name to test server error"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 2

      - log: "Sending request with invalid URL to test failure case"
      - get:
          url: "/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
