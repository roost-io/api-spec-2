# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=dc56f44070

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "topics_topic_name_get_others.csv"
    skipHeader: true
    fields:
      - "cluster_id"
      - "topic_name"
      - "include_authorized_operations"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Response Scenario"
    flow:
      - log: "Testing invalid authentication (expecting 401)"
        post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            # Asserting the presence of required properties in the response body
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 2

      - log: "Testing unauthorized access (expecting 403)"
        post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 2

      - log: "Testing bad request due to invalid query parameter (expecting 400)"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}?include_authorized_operations=invalid_boolean"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 2

      - log: "Testing non-existing resource (expecting 404)"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/non_existing_topic"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 2

      - log: "Testing rate limit exceeded (expecting 429)"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
      - think: 2

      - log: "Testing server-side error (expecting 500)"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "error_code"
            - hasProperty: "message"
