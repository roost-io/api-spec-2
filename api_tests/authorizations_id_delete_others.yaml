# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending a DELETE request with a valid ID but without authorization"
        delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: ""
          expect:
            - statusCode: 401 # Unauthorized
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - log: "Sending a DELETE request with an invalid ID"
        delete:
          url: "/authorizations/invalid-id"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Not Found
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - log: "Sending a DELETE request to a non-existent endpoint"
        delete:
          url: "/non-existent-endpoint"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Not Found
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1
