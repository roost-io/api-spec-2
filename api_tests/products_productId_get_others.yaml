# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Test various non-successful API requests"
    flow:
      - log: "Testing with an invalid product ID"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404
          - hasHeader: "Content-Type"
          - headerEquals: {"Content-Type": "application/json"}
        think: 1

      - log: "Testing with a missing product ID"
        get:
          url: "/products/"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404
          - hasHeader: "Content-Type"
          - headerEquals: {"Content-Type": "application/json"}
        think: 1

      - log: "Testing with an invalid URL"
        get:
          url: "/invalidURL"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404
          - hasHeader: "Content-Type"
          - headerEquals: {"Content-Type": "application/json"}
        think: 1

      - log: "Testing with unauthorized access"
        get:
          url: "/products/{{ productId }}"
        expect:
          - statusCode: 401
          - hasHeader: "Content-Type"
          - headerEquals: {"Content-Type": "application/json"}
        think: 1
