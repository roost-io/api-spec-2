# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /products/{productId}_get for http method type GET in artillery framework

# RoostTestHash=ab8efa24ed


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_success.csv"
    fields:
      - "productId"
    skipHeader: true
  environments:
    development:
      API_HOST: "http://dev.api.example.com"
    production:
      API_HOST: "https://api.example.com"

scenarios:
  - name: "Validate correct API behavior with valid input payloads"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          log: "Fetching product details for product ID {{ productId }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - matchesRegexp: { name: "^[a-zA-Z0-9 ]+$" }
            - matchesRegexp: { description: "^[a-zA-Z0-9 ]+$" }
            - headerEquals: { "Content-Type": "application/json" }
          capture:
            json: "$.id"
            as: "capturedProductId"
      - think: 1
