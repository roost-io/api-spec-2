# Test generated by RoostGPT for test credential-artillery-test-updated using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "Token"
    skipHeader: true

scenarios:
  - flow:
      # Invalid authentication token
      - get:
          url: "/me"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401 # Unauthorized
            - hasProperty: "message"
        think: 1

      # No authentication token
      - get:
          url: "/me"
          expect:
            - statusCode: 401 # Unauthorized
            - hasProperty: "message"
        think: 1

      # Invalid URL
      - get:
          url: "/invalid_url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Not Found
            - hasProperty: "message"
        think: 1

      # Valid request but expecting non-successful response
      - get:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200 # OK
            - hasProperty: "data"
            - hasProperty: "data.id"
            - hasProperty: "data.name"
            - hasProperty: "data.email"
        think: 1
