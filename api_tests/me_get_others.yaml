# Test generated by RoostGPT for test credential-artillery-test using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  expect: true
  payload:
    csv: me_get_others.csv
    skipHeader: true
scenarios:
  - scenario: Failure-Inducing Requests
    flow:
      - log: Sending request with invalid URL
        request:
          url: "/me/invalid-url"
          method: GET
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            statusCode: 404
      - think: 500
      - log: Sending request with invalid payload
        request:
          url: "/me"
          method: POST
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            invalid_field: invalid_value
          expect:
            statusCode: 400
            hasProperty: error
            hasProperty: description
      - think: 500
      - log: Sending request with missing authorization
        request:
          url: "/me"
          method: GET
          expect:
            statusCode: 401
            hasProperty: error
            hasProperty: description
      - think: 500
      - log: Sending request with invalid authorization
        request:
          url: "/me"
          method: GET
          headers:
            Token: invalid_token
          expect:
            statusCode: 401
            hasProperty: error
            hasProperty: description
