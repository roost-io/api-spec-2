# Test generated by RoostGPT for test credential-artillery-test-withlogs using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_others.csv"
    fields:
      - "description"
      - "error"
      - "field"
      - "schema_field"
      - "value"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Testing invalid payload for /me endpoint"
      - post:
          url: "/me"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalid_field: "invalid_value"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Testing authentication error for /me endpoint"
      - post:
          url: "/me"
          headers:
            Content-Type: "application/json"
            Authorization: "InvalidToken"
          json:
            valid_field: "valid_value"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Testing invalid URL for /me endpoint"
      - get:
          url: "/invalid_me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Testing method not allowed for /me endpoint"
      - put:
          url: "/me"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            valid_field: "valid_value"
          expect:
            - statusCode: 405
      - think: 1

      - log: "Testing request with missing required fields for /me endpoint"
      - post:
          url: "/me"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json: {}
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "schema_field"
            - hasProperty: "value"
