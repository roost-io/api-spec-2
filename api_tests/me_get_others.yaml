# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_others.csv"
    fields:
      - "invalidEmail"
      - "invalidToken"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      - log: "Testing with invalid authentication token"
        post:
          url: "/me"
          headers:
            Token: "{{ invalidToken }}"
          # Expecting 401 Authentication Required
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
        think: 1

      - log: "Testing with missing authentication token"
        post:
          url: "/me"
          # Expecting 401 Authentication Required
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
        think: 1

      - log: "Testing with invalid payload (bad email format)"
        post:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ invalidEmail }}"
          # Expecting 400 Bad Request
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
            - hasProperty: "description"
            - hasProperty: "error"
            - matchesRegexp: {"error": "^validation/.*$"}
        think: 1

      - log: "Testing with invalid URL"
        get:
          url: "/invalid-url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          # Expecting 404 Not Found
          expect:
            - statusCode: 404
        think: 1
