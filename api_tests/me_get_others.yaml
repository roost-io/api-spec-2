# Test generated by RoostGPT for test credential-artillery-test using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=ae6b8ce5ab

---
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  payload:
    file: me_get_others.csv
    skipHeader: true
  securitySchemes:
    ApiKeyAuth:
      type: 'apiKey'
      name: 'Token'
      in: 'header'
scenarios:
  - scenario:
      - get-me-invalid-url:
          method: GET
          url: https://invalid-url.com/me
          log: "Sending GET request to invalid URL"
          expect:
            statusCode: 404
      - get-me-invalid-auth:
          method: GET
          url: /me
          headers:
            Token: {{ $processEnvironment.AUTH_TOKEN }}
          log: "Sending GET request with invalid authorization"
          expect:
            statusCode: 401
      - get-me-invalid-payload:
          method: GET
          url: /me
          headers:
            Token: {{ $processEnvironment.AUTH_TOKEN }}
          payload:
            invalid_field: invalid_value
          log: "Sending GET request with invalid payload"
          expect:
            statusCode: 400
            hasProperty:
              error: true
              description: true
      - think: 500
