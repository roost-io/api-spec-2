# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    - path: "me_get_others.csv"
      fields:
        - "email"
        - "password"
      skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API Failure Test Scenario"
    flow:
      - log: "Testing with invalid authentication token"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasProperty: 'description'
          # Expecting 401 Unauthorized with error and description in the response body

      - think: 1

      - log: "Testing with missing authentication token"
      - post:
          url: "/me"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasProperty: 'description'
          # Expecting 401 Unauthorized with error and description in the response body

      - think: 1

      - log: "Testing with invalid payload structure"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidField: "invalid_value"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'
          # Expecting 400 Bad Request with error and description in the response body

      - think: 1

      - log: "Testing with invalid URL"
      - post:
          url: "/invalid_endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          # Expecting 404 Not Found due to invalid URL

      - think: 1

      - log: "Testing with valid token but invalid payload value"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "not_an_email"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'
            - hasProperty: 'schema_field'
            - hasProperty: 'value'
          # Expecting 400 Bad Request with detailed validation error in the response body
