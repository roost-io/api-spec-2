# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_others.csv"
    skipHeader: true
    fields:
      - "invalidEmail"
      - "emptyToken"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      # Invalid Authentication Token
      - post:
          url: "/me"
          headers:
            Token: "{{ emptyToken }}"
          json:
            email: "valid@example.com"
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
          comment: "Expecting 401 due to invalid authentication token."
      - think: 1

      # Invalid Email Format
      - post:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ invalidEmail }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - matchesRegexp: 
                property: "error"
                pattern: "^validation/.*$"
            - hasHeader: "api-version"
          comment: "Expecting 400 due to invalid email format."
      - think: 1

      # Missing Required Fields
      - post:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: ""
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
          comment: "Expecting 400 due to missing required fields."
      - think: 1

      # Invalid URL
      - get:
          url: "/invalidEndpoint"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          comment: "Expecting 404 due to invalid URL."
      - think: 1
