# Test generated by RoostGPT for test credential-artillery-test using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=ae6b8ce5ab

---
config:
  target: "{{ $processEnvironment.API_HOST }}"
  expect: true
  payload:
    me_get_others.csv:
      skipHeader: true
      fields:
        - id
        - name
        - email
  auth:
    ApiKeyAuth:
      Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - scenario: Failure-Inducing Requests
    flow:
      - log: Sending request with invalid URL
        request:
          url: "/me/invalid-url"
          method: GET
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            statusCode: 404
      - think: 1000
      - log: Sending request with invalid payload
        request:
          url: "/me"
          method: POST
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            invalid_field: invalid_value
          expect:
            statusCode: 400
      - think: 1000
      - log: Sending request without required field
        request:
          url: "/me"
          method: POST
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            name: John Doe
          expect:
            statusCode: 400
      - think: 1000
      - log: Sending request with invalid email format
        request:
          url: "/me"
          method: POST
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            name: John Doe
            email: invalid_email
          expect:
            statusCode: 400
      - think: 1000
      - log: Sending request with invalid authentication
        request:
          url: "/me"
          method: GET
          headers:
            Token: invalid_token
          expect:
            statusCode: 401
