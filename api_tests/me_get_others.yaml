# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "API failure cases"
    flow:
      - log: "Attempting to access API with incorrect URL"
        get:
          url: "/invalid_url"
          expect:
            - statusCode: 404
        think: 1

      - log: "Attempting to access API with no authentication"
        get:
          url: "/me"
          expect:
            - statusCode: 401
        think: 1

      - log: "Attempting to access API with invalid authentication"
        get:
          url: "/me"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 403
        think: 1

      - log: "Attempting to access API with valid authentication but invalid payload"
        post:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            password: "invalidpassword"
          expect:
            - statusCode: 400
        think: 1

      - log: "Attempting to access API with valid authentication and payload but incorrect method"
        put:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 405
        think: 1
