# Test generated by RoostGPT for test credential-artillery-test-updated using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending request with incorrect URL"
        get:
          url: "/invalid_url"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404

        think: 1

      - log: "Sending request with invalid payload"
        post:
          url: "/me"
          json:
            username: "invalid_username"
            password: "invalid_password"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                error: "Invalid payload"

        think: 1

      - log: "Sending request without authentication"
        get:
          url: "/me"
          headers:
            Token: ""
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: 
                error: "Authentication required"

        think: 1

      - log: "Sending request with invalid authentication"
        get:
          url: "/me"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 403
            - hasProperty: "error"
            - equals: 
                error: "Invalid authentication token"

        think: 1

      - log: "Sending request with valid payload and authentication but to a non-existing resource"
        get:
          url: "/me/non_existing_resource"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals: 
                error: "Resource not found"

        think: 1
