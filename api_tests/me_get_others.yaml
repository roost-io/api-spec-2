# Test generated by RoostGPT for test credential-artillery-test-updated using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "Token"
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      # Testing with an invalid URL
      - get:
          url: "/invalid_url"
          expect:
            - statusCode: 404
          think: 1
          
      # Testing with invalid Token in header
      - get:
          url: "/me"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 401
          think: 1

      # Testing with missing Token in header
      - get:
          url: "/me"
          headers:
            Token: ""
          expect:
            - statusCode: 401
          think: 1

      # Testing with valid Token but expecting a non-success response
      - get:
          url: "/me"
          headers:
            Token: "{{ Token }}"
          expect:
            - statusCode: 500
          think: 1
