# Test generated by RoostGPT for test credential-artillery-test using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=ae6b8ce5ab

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  payload:
    file: me_get_others.csv
    skipHeader: true
scenarios:
  - scenario: Failure Cases
    flow:
      - get:
          url: /me
          log: "GET /me - Expecting 401 Unauthorized"
          expect:
            - statusCode: 401
            - hasHeader: Token
            - headerEquals: Token: {{$processEnvironment.AUTH_TOKEN}}
      - post:
          url: /me
          log: "POST /me - Expecting 400 Bad Request (Invalid payload)"
          expect:
            - statusCode: 400
            - hasProperty: error
            - hasProperty: description
            - equals: error: validation/email-pattern
            - equals: description: "Email address is invalid"
          payload:
            email: invalid-email
      - delete:
          url: /me
          log: "DELETE /me - Expecting 401 Unauthorized"
          expect:
            - statusCode: 401
            - hasHeader: Token
            - headerEquals: Token: {{$processEnvironment.AUTH_TOKEN}}
      - put:
          url: /me
          log: "PUT /me - Expecting 400 Bad Request (Invalid payload)"
          expect:
            - statusCode: 400
            - hasProperty: error
            - hasProperty: description
            - equals: error: validation/email-pattern
            - equals: description: "Email address is invalid"
          payload:
            email: invalid-email
      - get:
          url: /me/invalid-endpoint
          log: "GET /me/invalid-endpoint - Expecting 404 Not Found"
          expect:
            - statusCode: 404
      - think: 500
