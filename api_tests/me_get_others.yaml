# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_others.csv"
    fields:
      - "invalidEmail"
      - "invalidField"
    skipHeader: true
  environments:
    staging:
      API_HOST: "https://api.staging.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Account API negative test cases"
    flow:
      - log: "Sending request with invalid URL to test bad request response"
      - get:
          url: "/invalid-url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
          capture:
            json: "$.headers.api-version"
            as: "apiVersion"

      - think: 1

      - log: "Sending request with missing Authorization header to test authentication required response"
      - get:
          url: "/me"
          headers: {}
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
          capture:
            json: "$.headers.api-version"
            as: "apiVersion"

      - think: 1

      - log: "Sending request with invalid Authorization token to test authentication required response"
      - get:
          url: "/me"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
          capture:
            json: "$.headers.api-version"
            as: "apiVersion"
