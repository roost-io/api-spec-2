# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /products_get for http method type GET in artillery framework

# RoostTestHash=70199667a6


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_get_success.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.example.com"
    production:
      target: "https://example.com"

scenarios:
  - name: "Validate correct API behavior with valid input payloads"
    flow:
      - get:
          url: "/products"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          log: "Fetching all products"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
            - headerEquals: { header: "content-type", value: "application/json" }
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - matchesRegexp: { property: "name", pattern: "^[a-zA-Z0-9 ]+$" }
            - jmespath: "length([?id=='{{ id }}']) == `1`"
          # Think time after the request
          think: 1
