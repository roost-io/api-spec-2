# Test generated by RoostGPT for test credential-artillery-test-withlogs using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_success.csv"
    fields:
      - "id"
      - "name"
      - "email"
      - "createdAt"
      - "updatedAt"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - flow:
      - get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
          log: "Requesting account details"
      - think: 1

      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
          log: "Updating account details"
      - think: 1

      - post:
          url: "/logout"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
          log: "Logging out"
      - think: 1
