# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "http://localhost:8080"
  plugins:
    expect: {}
  payload:
    path: "Delete_an_existing_product.csv"
    skipHeader: true
    fields:
      - "productId"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Delete an existing product"
    flow:
      # Assert that the product exists before deletion
      - get:
          url: "/api/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "productId"
          beforeRequest: "log"
          afterResponse: "log"
      - think: 1

      # Delete the product and assert the deletion was successful
      - delete:
          url: "/api/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
          beforeRequest: "log"
          afterResponse: "log"
      - think: 1

      # Assert that the product no longer exists after deletion
      - get:
          url: "/api/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          beforeRequest: "log"
          afterResponse: "log"
