# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}/api"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "Delete_an_existing_product.csv"
    fields:
      - "productId"
    skipHeader: true
  variables:
    authToken: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Delete an existing product"
    flow:
      - log: "Starting scenario to delete an existing product"
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          expect:
            - statusCode: 200
            - hasProperty: "productId"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
      - think: 1
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          expect:
            - statusCode: 200
      - think: 1
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          expect:
            - statusCode: 404
      - log: "Product deletion scenario completed successfully"
