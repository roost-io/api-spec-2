# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /api/device/camera/feature/alert_post for http method type POST in artillery framework

# RoostTestHash=c211c7fbe6


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 3600
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "alert_post_success.csv"
    fields:
      - "sn"
      - "cameraId"
      - "timestamp"
      - "confidence"
      - "alert"
      - "subAlert"
      - "photoUrl"
      - "videoUrl"
    skipHeader: true
  environments:
    dev:
      target: "https://dev.example.com"

scenarios:
  - name: "Feature alert scenario"
    flow:
      - post:
          url: "/api/device/camera/feature/alert"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            sn: "{{ sn }}"
            cameraId: "{{ cameraId }}"
            timestamp: "{{ timestamp }}"
            confidence: "{{ confidence }}"
            alert: "{{ alert }}"
            subAlert: "{{ subAlert }}"
            photoUrl: "{{ photoUrl }}"
            videoUrl: "{{ videoUrl }}"
          expect:
            - statusCode: 201
            - hasProperty: "alerts"
            - hasProperty: "alerts[0].status"
          # Alert creation successful. Expect a 201 status.
          capture:
            - json: "$.alerts[0].id"
              as: "alertId"
      - log: "Created alert with ID: {{ alertId }}"
      - think: 5
