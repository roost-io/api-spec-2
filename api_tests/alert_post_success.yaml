# Test generated by RoostGPT for test credential-artillery-test using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /api/device/camera/feature/alert_post for http method type POST in artillery framework

# RoostTestHash=c211c7fbe6


--- Corrected Artillery YAML Test Case Start ---
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  payload:
    csv: alert_post_success.csv
    skipHeader: true
scenarios:
  - flow:
      - get:
          url: /api/device/camera/feature/alert
          headers:
            Authorization: Bearer {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 200
            contentType: application/json
            hasHeader: content-type
            hasHeader: content-length
            hasHeader: date
            hasHeader: etag
            hasHeader: server
            hasHeader: transfer-encoding
            hasHeader: vary
            hasHeader: x-content-type-options
            hasHeader: x-frame-options
            hasHeader: x-xss-protection
            hasProperty: id
            hasProperty: sn
            hasProperty: cameraId
            hasProperty: timestamp
            hasProperty: confidence
            hasProperty: alert
            hasProperty: subAlert
            hasProperty: photoUrl
            hasProperty: videoUrl
            hasProperty: createdAt
            hasProperty: updatedAt
            hasProperty: deletedAt
      - post:
          # Comment: Creating a new alert with valid payload
          url: /api/device/camera/feature/alert
          headers:
            Authorization: Bearer {{$processEnvironment.AUTH_TOKEN}}
          payload:
            sn: ${sn}
            cameraId: ${cameraId}
            timestamp: ${timestamp}
            confidence: ${confidence}
            alert: ${alert}
            subAlert: ${subAlert}
            photoUrl: ${photoUrl}
            videoUrl: ${videoUrl}
          expect:
            statusCode: 201
            contentType: application/json
            hasHeader: content-type
            hasHeader: content-length
            hasHeader: date
            hasHeader: etag
            hasHeader: server
            hasHeader: transfer-encoding
            hasHeader: vary
            hasHeader: x-content-type-options
            hasHeader: x-frame-options
            hasHeader: x-xss-protection
            hasProperty: id
            hasProperty: sn
            hasProperty: cameraId
            hasProperty: timestamp
            hasProperty: confidence
            hasProperty: alert
            hasProperty: subAlert
            hasProperty: photoUrl
            hasProperty: videoUrl
            hasProperty: createdAt
            hasProperty: updatedAt
            hasProperty: deletedAt
      - think: 500

--- Corrected Artillery YAML Test Case End ---
