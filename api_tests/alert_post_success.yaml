# Test generated by RoostGPT for test credential-artillery-test using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /api/device/camera/feature/alert_post for http method type POST in artillery framework

# RoostTestHash=c211c7fbe6


config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    enabled: true
  payload:
    csv: alert_post_success.csv
    skipHeader: true
scenarios:
  - flow:
      - get:
          url: /api/device/camera/feature/alert
          headers:
            Authorization: Bearer {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 200
            contentType: application/json
            hasHeader: Content-Type
            headerEquals: application/json
            hasProperty: success
            equals: true
      - think: 500
      - post:
          url: /api/device/camera/feature/alert
          headers:
            Authorization: Bearer {{$processEnvironment.AUTH_TOKEN}}
          payload:
            sn: {{$payload.sn}}
            cameraId: {{$payload.cameraId}}
            timestamp: {{$payload.timestamp}}
            confidence: {{$payload.confidence}}
            alert: {{$payload.alert}}
            subAlert: {{$payload.subAlert}}
            photoUrl: {{$payload.photoUrl}}
            videoUrl: {{$payload.videoUrl}}
          expect:
            statusCode: 201
            contentType: application/json
            hasHeader: Content-Type
            headerEquals: application/json
            hasProperty: success
            equals: true
