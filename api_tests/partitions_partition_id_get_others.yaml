# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3e9c4dd20d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  payload:
    path: "partitions_partition_id_get_others.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "partition_id"
    skipHeader: true
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "API test scenario"
    flow:
      - log: "Sending a GET request with invalid cluster_id"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Sending a GET request with invalid topic_name"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Sending a GET request with invalid partition_id"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Sending a GET request with invalid URL"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Sending a GET request without authorization"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          expect:
            - statusCode: 401
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Sending a GET request with invalid authorization"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
