# Test generated by RoostGPT for test nobelprize-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=4940f5c55b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "nobelPrizes_get_others.csv"
    fields:
      - "nobelPrizeYear"
      - "nobelPrizeCategory"
      - "sort"
      - "offset"
      - "limit"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API Failure Responses Testing"
    flow:
      - log: "Testing with invalid authorization token"
        auth: "Bearer invalid_token"
        get:
          url: "/2.1/nobelPrizes"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401 # Unauthorized
            - hasProperty: "message"

      - think: 2

      - log: "Testing with malformed query parameters"
        get:
          url: "/2.1/nobelPrizes?nobelPrizeYear=abcd"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Bad request
            - hasProperty: "message"

      - think: 2

      - log: "Testing with invalid 'sort' parameter value"
        get:
          url: "/2.1/nobelPrizes?sort=invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 422 # Unprocessable Entity
            - hasProperty: "message"

      - think: 2

      - log: "Testing with invalid 'nobelPrizeCategory' parameter value"
        get:
          url: "/2.1/nobelPrizes?nobelPrizeCategory=unknown"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 422 # Unprocessable Entity
            - hasProperty: "message"

      - think: 2

      - log: "Testing with invalid URL endpoint"
        get:
          url: "/2.1/invalidEndpoint"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Not Found
            - hasProperty: "message"

      - think: 2

      - log: "Testing with missing required parameters"
        get:
          url: "/2.1/nobelPrizes?offset=&limit="
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Bad request
            - hasProperty: "message"
