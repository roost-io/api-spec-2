# Test generated by RoostGPT for test qlue-single-api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /UpdateCustomerInfo_put for http method type PUT in artillery framework

# RoostTestHash=89ddb90785


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "UpdateCustomerInfo_put_others.csv"
    fields: ["customerId", "email", "alternatePhoneNumber", "isAlternateMobileNumber", "phone", "isPrimaryMobileNumber", "location"]
    skipHeader: true

scenarios:
  - name: "Scenario for Non-Success API Responses"
    flow:
      # Invalid payload request
      - post:
          url: "/UpdateCustomerInfo"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "invalid_phone_number"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          headers:
            koreUserId: "invalid_user_id"
            botId: "invalid_bot_id"
            accountId: "invalid_account_id"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: 123456
          expect:
            - statusCode: 400
        think: 1

      # Incorrect URL request
      - post:
          url: "/InvalidUpdateCustomerInfo"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          headers:
            koreUserId: "valid_user_id"
            botId: "valid_bot_id"
            accountId: "valid_account_id"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: 123456
          expect:
            - statusCode: 404
        think: 1

      # Unauthorized request
      - post:
          url: "/UpdateCustomerInfo"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          headers:
            koreUserId: "valid_user_id"
            botId: "valid_bot_id"
            accountId: "valid_account_id"
            Authorization: "invalid_auth_token"
            X-Auth-token: "invalid_auth_token"
            X-CORRELATION-ID: 123456
          expect:
            - statusCode: 401
        think: 1
