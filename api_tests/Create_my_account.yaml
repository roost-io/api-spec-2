# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "Create_my_account.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Create my account"
    flow:
      - post:
          url: "/accounts"
          headers:
            commit-hash: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: "email"
          log: "Sent a POST request to /accounts with valid payload and received a response"
          think: 1
      - get:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "email"
          log: "Verified the account created using GET request for /me"
          think: 1
