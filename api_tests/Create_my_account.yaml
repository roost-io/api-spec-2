# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "Create_my_account.csv"
    fields:
      - "accounts_body"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Create my account"
    flow:
      # Send a POST request to /accounts with valid payload
      - post:
          url: "/accounts"
          json:
            accounts_body: "{{ accounts_body }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            # Expect a 201 status code indicating successful account creation
            - statusCode: 201
            # Expect the response to contain the account body
            - hasProperty: "accounts_body"
      - think: 1

      # Send a GET request to /me to verify the account details
      - get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            # Expect a 200 status code indicating successful retrieval of account details
            - statusCode: 200
            # Expect the response to contain the account body
            - hasProperty: "accounts_body"
