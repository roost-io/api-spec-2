# Test generated by RoostGPT for test credwallet-integration-artillery-updated using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payloads:
    - path: "Create_my_account.csv"
      fields:
        - "accounts_body"
      skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Create my account"
    flow:
      - post:
          url: "/accounts"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            accounts_body: "{{ accounts_body }}"
          expect:
            # Expect a 201 status code indicating successful account creation
            - statusCode: 201
            # Expect the response to include an 'id' property, signifying the account's unique identifier
            - hasProperty: "id"
      - think: 1
      # Verify the account creation by sending a GET request
      - get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            # Expect a 200 status code indicating successful retrieval of account details
            - statusCode: 200
            # Expect the response to include an 'id' property, confirming the account's unique identifier
            - hasProperty: "id"
      - log: "Account has been successfully created and verified"
