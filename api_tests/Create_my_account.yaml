# Test generated by RoostGPT for test credwallet-integration-artillery-updated using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "Create_my_account.csv"
    fields:
      - "email"
    skipHeader: true
scenarios:
  - name: "Create my account"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          expect:
            - statusCode: 201
              hasProperty: ['email']
            - hasHeader: ['api-version']
              headerEquals: ['api-version', '1.0.0']
            - hasHeader: ['commit-hash']
              headerEquals: ['commit-hash', '928d28d']
          capture:
            json: "$.email"
            as: "email"
          log: "Creating an account with the specified information"
      - think: 1
      - get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
              hasProperty: ['id', 'did', 'name', 'image', 'email', 'settings', 'createdAt', 'updatedAt']
            - hasHeader: ['api-version']
              headerEquals: ['api-version', '1.0.0']
          log: "Verifying the account created"
