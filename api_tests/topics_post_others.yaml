# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fa240a7735

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "topics_post_others.csv"
    fields:
      - "cluster_id"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "API failure and edge cases testing"
    flow:
      - log: "Testing for invalid URL"
        post:
          url: "/kafka/v3/clusters/INVALID/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
      - log: "Testing for unauthorized access"
        post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "INVALID"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1
      - log: "Testing for forbidden access"
        post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
            - hasProperty: "error"
      - think: 1
      - log: "Testing for rate limit threshold"
        post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
            - hasProperty: "error"
      - think: 1
      - log: "Testing for server errors"
        post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "error"
