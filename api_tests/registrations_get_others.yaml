# ********RoostGPT********
#
Test generated by RoostGPT for test qlue-single-api-test using AI Type Open AI and AI Model gpt-4

Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework
RoostTestHash=49b35fa9c1


#

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
  plugins:
    expect: {}
scenarios:
  - name: "Test non-successful API responses"
    flow:
      # Test with invalid id
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      # Test with invalid credential-url
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url=invalid&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      # Test with invalid size
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credential-url }}&size=100&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      # Test with invalid offset
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset=-1"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
