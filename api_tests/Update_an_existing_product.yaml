# Test generated by RoostGPT for test credwallet-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "Update_an_existing_product.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Update an existing product" # Scenario name
    flow:
      - log: "Given there is an existing product with ID 1"
      - get: # Step to get the existing product details
          url: "/products/1"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect: # Assertions to check the response
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
      - log: "The client provides the following product data"
      - think: 1

      - put: # Step to update the existing product
          url: "/products/1"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json: # Payload for the update request
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect: # Assertions to check the response
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - equals: # Check if the updated details are as expected
                name: "{{ name }}"
                description: "{{ description }}"
                price: "{{ price }}"
      - log: "The product with ID 1 should be updated with the provided details"
      - think: 1
