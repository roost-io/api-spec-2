# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /locateProfile_post for http method type POST in artillery framework

# RoostTestHash=2b60fcac41


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "locateProfile_post_others.csv"
    fields:
      - "customerId"
      - "ssNumber"
      - "ssnShort"
      - "accountNumber"
    skipHeader: true
    order: sequence
  phases:
    - duration: 60
      arrivalRate: 5
  defaults:
    headers:
      Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - post:
          url: "/locateProfile"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
            accountNumber: "{{ accountNumber }}"
          expect:
            - statusCode: 400
          capture:
            - json: "$"
              as: "responseBody"
        think: 1
      - log: "Posted invalid data to /locateProfile, expected 400 status code"
      
      - post:
          url: "/invalidURL"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
            accountNumber: "{{ accountNumber }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Posted to invalid URL, expected 404 status code"

      - post:
          url: "/locateProfile"
          headers:
            Authorization: "Invalid Token"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
            accountNumber: "{{ accountNumber }}"
          expect:
            - statusCode: 401
        think: 1
      - log: "Posted with invalid Authorization token, expected 401 status code"
