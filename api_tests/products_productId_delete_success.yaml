# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework

# RoostTestHash=befb82c928


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "products_productId_delete_success.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete product scenario"
    flow:
      - log: "Sending DELETE request to delete a product"
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
            - headerEquals:
                name: "content-type"
                value: "application/json"
      - think: 1

      - log: "Sending GET request to confirm product deletion"
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals:
                json: "$.message"
                value: "Product not found"
      - think: 1
