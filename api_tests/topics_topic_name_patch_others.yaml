# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=1f79c3935d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "topics_topic_name_patch_others.csv"
    fields:
      - "topic_name"
      - "cluster_id"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Patch API negative tests"
    flow:
      - log: "Invalid URL test"
        patch:
          url: "/kafka/v3/cluster/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Unauthorized access test"
        patch:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Access forbidden test"
        patch:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
      - think: 1
      - log: "Rate limit test"
        patch:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
      - think: 1
      - log: "Server error test"
        patch:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
