# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=b9526a5d59

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "topics_topic_name_delete_others.csv"
    fields:
      - "cluster_id"
      - "topic_name"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete Kafka Topic Failure and Edge Cases Scenario"
    flow:
      # Invalid URL test case
      - log: "Testing with an invalid URL"
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/invalid/{{ topic_name }}"
          headers:
            Authorization: "Bearer INVALID_TOKEN"
          expect:
            - statusCode: 404
            - hasProperty: 'message'
            - hasProperty: 'error_code'
      - think: 1

      # Authorization failure test case
      - log: "Testing with invalid authorization"
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: 'message'
            - hasProperty: 'error_code'
      - think: 1

      # Bad request test case
      - log: "Testing with a bad request error"
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidField: "invalid_value"
          expect:
            - statusCode: 400
            - hasProperty: 'message'
            - hasProperty: 'error_code'
      - think: 1

      # Forbidden request test case
      - log: "Testing with forbidden request error"
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
            - hasProperty: 'message'
            - hasProperty: 'error_code'
      - think: 1

      # Rate limit exceeded test case
      - log: "Testing rate limiting"
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
      - think: 1

      # Server-side error test case
      - log: "Testing server-side error"
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: 'message'
            - hasProperty: 'error_code'
