# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=b9526a5d59

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "topics_topic_name_delete_others.csv"
    fields:
      - "cluster_id"
      - "topic_name"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete Topic Failure Scenarios"
    flow:
      - log: "Attempting to delete a topic with invalid authentication"
        post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            # Expecting client authentication error with error code 40101
            - hasProperty: "error_code"
            - equals: {error_code: 40101}
        think: 2

      - log: "Attempting to delete a topic with incorrect cluster_id"
        delete:
          url: "/kafka/v3/clusters/invalid_cluster_id/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            # Expecting unknown cluster error with error code 404
            - hasProperty: "error_code"
            - equals: {error_code: 404}
        think: 2

      - log: "Attempting to delete a topic with invalid authorization (expired token)"
        delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer expired_token"
          expect:
            - statusCode: 403
            # Expecting client authorization error with error code 40301
            - hasProperty: "error_code"
            - equals: {error_code: 40301}
        think: 2

      - log: "Attempting to delete a topic with non-existing topic_name"
        delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/non_existing_topic"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            # Expecting unknown topic error with error code 40403
            - hasProperty: "error_code"
            - equals: {error_code: 40403}
        think: 2

      - log: "Attempting to delete a topic with rate limit exceeded"
        delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
            # Expecting rate limit error with error code 429
        think: 2

      - log: "Attempting to delete a topic with server-side error"
        delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            # Expecting internal server error with error code 500
            - hasProperty: "error_code"
            - equals: {error_code: 500}
