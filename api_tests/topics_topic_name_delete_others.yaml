# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=5f8e80468a

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 50
  payload:
    path: "topics_topic_name_delete_others.csv"
    fields:
      - "cluster_id"
      - "topic_name"
    skipHeader: true

scenarios:
  - name: "API failure and edge cases testing"
    flow:
      - log: "Testing with invalid URL"
        delete:
          url: "/invalid/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
              hasProperty: ['id', 'status', 'code', 'title', 'detail', 'source', 'source.pointer', 'source.parameter', 'error_code', 'message']
            - hasHeader: 'Content-Type'
      - think: 1

      - log: "Testing with invalid Authorization"
        delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Invalid Token"
          expect:
            - statusCode: 401
              hasProperty: ['id', 'status', 'code', 'title', 'detail', 'source', 'source.pointer', 'source.parameter', 'error_code', 'message']
            - hasHeader: 'Content-Type'
      - think: 1

      - log: "Testing with missing required parameter (topic_name)"
        delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
              hasProperty: ['id', 'status', 'code', 'title', 'detail', 'source', 'source.pointer', 'source.parameter', 'error_code', 'message']
            - hasHeader: 'Content-Type'
      - think: 1

      - log: "Testing with exceeding rate limit"
        delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
            - hasHeader: 'Content-Type'
      - think: 1

      - log: "Testing with server error"
        delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
              hasProperty: ['id', 'status', 'code', 'title', 'detail', 'source', 'source.pointer', 'source.parameter', 'error_code', 'message']
            - hasHeader: 'Content-Type'
      - think: 1
