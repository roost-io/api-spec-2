# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      - log: "Sending a DELETE request with an invalid product ID"
        # Expected outcome: 404 Not Found
        delete:
          url: "/products/{{ productId }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN_NAME }}: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Sending a DELETE request to an invalid URL"
        # Expected outcome: The request will fail due to an incorrect URL
        delete:
          url: "/invalidUrl/{{ productId }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN_NAME }}: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Sending a DELETE request without authorization token"
        # Expected outcome: The request will fail due to missing authorization
        delete:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401
        think: 1

      - log: "Sending a DELETE request with an expired authorization token"
        # Expected outcome: The request will fail due to expired or invalid authorization
        delete:
          url: "/products/{{ productId }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN_NAME }}: "expired_or_invalid_token"
          expect:
            - statusCode: 403
        think: 1
