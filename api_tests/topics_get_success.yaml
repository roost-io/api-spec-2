# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /kafka/v3/clusters/{cluster_id}/topics_get for http method type GET in artillery framework
# RoostTestHash=dbb5ec3318

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "topics_get_success.csv"
    fields:
      - "cluster_id"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Test /kafka/v3/clusters/{cluster_id}/topics API"
    flow:
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'id'
            - hasProperty: 'status'
            - hasProperty: 'code'
            - hasProperty: 'title'
            - hasProperty: 'detail'
            - hasProperty: 'source'
            - hasProperty: 'source.pointer'
            - hasProperty: 'source.parameter'
            - hasProperty: 'error_code'
            - hasProperty: 'message'
          log: "Expected 200 status code and required properties in the response body"
        think: 1
