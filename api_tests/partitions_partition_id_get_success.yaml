# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}_get for http method type GET in artillery framework
# RoostTestHash=3e9c4dd20d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    - statusCode: 200
  processor: "./processor.js"
  plugins:
    expect: {}
  payload:
    path: "partitions_partition_id_get_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "partition_id"
    skipHeader: true

scenarios:
  - name: "Test /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id} API"
    flow:
      - log: "Starting the test for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id} API"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
      - log: "Finished the test for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id} API"
