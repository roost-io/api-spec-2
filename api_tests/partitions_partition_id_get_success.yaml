# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}_get for http method type GET in artillery framework

# RoostTestHash=0ecd20dc17


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "partitions_partition_id_get_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "partition_id"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Validate correct API behavior with valid input payloads"
    flow:
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "cluster_id"
            - hasProperty: "topic_name"
            - hasProperty: "partition_id"
            - hasProperty: "leader"
            - hasProperty: "replicas"
            - hasProperty: "reassignment"
          log: "Getting partition with the given partition_id"
      - think: 1 # Think time after each request
