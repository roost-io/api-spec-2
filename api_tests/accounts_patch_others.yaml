# ********RoostGPT********

# Test generated by RoostGPT for test gpt-turbo using AI Type Open AI and AI Model gpt-4-turbo-preview
# 
# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_patch_others.csv"
    fields:
      - "email"
    skipHeader: true
  variables:
    emailInvalid: "invalidemail"
    authToken: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ emailInvalid }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - matchesRegexp: "Invalid email format"
      - think: 1

      - post:
          url: "/accounts"
          json: {}
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - matchesRegexp: "Email is required"
      - think: 1

      - post:
          url: "/accounts"
          json:
            email: "validemail@example.com"
          headers:
            Authorization: "Bearer invalidtoken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - matchesRegexp: "Unauthorized"
      - think: 1

      - post:
          url: "/invalid_endpoint"
          json:
            email: "validemail@example.com"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - matchesRegexp: "Not Found"
      - think: 1

