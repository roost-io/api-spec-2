# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions_get for http method type GET in artillery framework

# RoostTestHash=4ee5afee1b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "partitions_get_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "List Kafka Partitions Scenario"
    flow:
      - log: "Requesting the list of partitions for the specified topic."
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$.metadata.self"
              as: "self_link"
            - json: "$.data"
              as: "partition_data"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "data"
            - hasHeader: "Content-Type"
      - think: 1

      - log: "Validating the response contains the correct cluster_id and topic_name."
      - get:
          url: "{{ self_link }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "data"
            - hasHeader: "Content-Type"
          match:
            - json: "$.data[0].cluster_id"
              equals: "{{ cluster_id }}"
            - json: "$.data[0].topic_name"
              equals: "{{ topic_name }}"
      - think: 1
