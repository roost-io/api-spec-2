# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions_get for http method type GET in artillery framework

# RoostTestHash=4ee5afee1b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "partitions_get_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
    skipHeader: true
  environments:
    development:
      target: "http://localhost:3000"
  plugins:
    expect: {}

scenarios:
  - name: "Validate correct API behavior with valid input payloads"
    flow:
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "data"
          log: "Request to list partitions for a topic"
      - think: 1
      - get:
          # Another GET request with different parameters or endpoint can be added here
          # Make sure to include the log and think keys as shown in the first request
          # Include the necessary expect assertions to validate the response
          # Example:
          # url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/configurations"
          # headers:
          #   Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          # log: "Request to retrieve topic configurations"
      - think: 1
      # More requests can be added following the same pattern
