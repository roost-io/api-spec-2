# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions_get for http method type GET in artillery framework
# RoostTestHash=7a4b4c4432

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "partitions_get_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
  plugins:
    expect: {}

scenarios:
  - name: "Get partitions for a cluster topic"
    flow:
      - log: "Starting test scenario for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions API endpoint"
      - think: 1

      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - header: "Content-Type"
              as: "content_type"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
            - hasProperty: "message"
          log: "GET request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions was successful"
      - think: 1
