# Test generated by RoostGPT for test qlue-single-api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /GetAccountInfo_post for http method type POST in artillery framework

# RoostTestHash=8a95fbc531


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetAccountInfo_post_others.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
      - "Authorization"
      - "X-Auth-token"
      - "X-CORRELATION-ID"
    skipHeader: true
  variables:
    auth: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Non-success API responses"
    flow:
      # Incorrect koreUserId
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "invalid"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "Bearer {{ auth }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: 1045300
          expect:
            - statusCode: 400
        think: 1

      # Incorrect botId
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "invalid"
            accountId: "{{ accountId }}"
            Authorization: "Bearer {{ auth }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: 1045300
          expect:
            - statusCode: 400
        think: 1

      # Incorrect accountId
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "invalid"
            Authorization: "Bearer {{ auth }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: 1045300
          expect:
            - statusCode: 400
        think: 1

      # Incorrect Authorization
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "Bearer invalid"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: 1045300
          expect:
            - statusCode: 401
        think: 1

      # Incorrect X-Auth-token
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "Bearer {{ auth }}"
            X-Auth-token: "invalid"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: 1045300
          expect:
            - statusCode: 401
        think: 1

      # Incorrect X-CORRELATION-ID
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "Bearer {{ auth }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "invalid"
          json:
            customerId: 1045300
          expect:
            - statusCode: 400
        think: 1
