# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=dbb5ec3318

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "topics_get_others.csv"
    fields:
      ["cluster_id"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
  http:
    timeout: 20
scenarios:
  - name: "Test Failure, Edge and Error cases for /kafka/v3/clusters/{cluster_id}/topics API"
    flow:
      - log: "Testing the API when cluster_id is not provided"
        get:
          url: "/kafka/v3/clusters//topics"
          headers:
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
        think: 1
      - log: "Testing the API when Authorization token is not provided"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          expect:
            - statusCode: 401
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
        think: 1
      - log: "Testing the API when invalid Authorization token is provided"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 403
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
        think: 1
      - log: "Testing the API when rate limit is reached"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
        think: 1
      - log: "Testing the API for server error"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
        think: 1
