# ********RoostGPT********

# Test generated by RoostGPT for test gpt-turbo using AI Type Open AI and AI Model gpt-4-turbo-preview
# 
# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_patch_success.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}
  variables:
    AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Verify successful API operations"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals:
                api-version: "1.0.0"
          comment: "Validates successful login with a valid email, expects OTP email and API version header."
      - think: 1

