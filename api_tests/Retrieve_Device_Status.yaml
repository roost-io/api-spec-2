# Test generated by RoostGPT for test qlue-integration-artillery using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my-functions.js"
  plugins:
    expect: {}
  payload:
    path: "Retrieve_Device_Status.csv"
    fields:
      - "JetsonID"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        authToken: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Retrieve Device Status"
    flow:
      - log: "Retrieve Device Status"
      - get:
          url: "/GetStatus?JetsonID={{ JetsonID }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - equals: 
                status: "valid"
      - think: 1
