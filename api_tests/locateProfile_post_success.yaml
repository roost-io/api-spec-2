# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /locateProfile_post for http method type POST in artillery framework

# RoostTestHash=2b60fcac41


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "locateProfile_post_success.csv"
    fields:
      - "customerId"
      - "ssNumber"
      - "ssnShort"
      - "accountNumber"
    skipHeader: true
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "locateProfile API Test"
    flow:
      - post:
          url: "/locateProfile"
          headers:
            Content-Type: "application/json"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
            accountNumber: "{{ accountNumber }}"
          expect:
            - statusCode: 200
            - hasProperty: "accountNumber"
            - hasProperty: "cardDetails"
            - hasProperty: "personalQuestions"
            - hasProperty: "ssNumber"
            - hasProperty: "ssnShort"
            - hasProperty: "alternatePhoneNumber"
            - hasProperty: "customerId"
            - hasProperty: "email"
            - hasProperty: "identificationNumber"
            - hasProperty: "identificationType"
            - hasProperty: "image"
            - hasProperty: "isAlternateMobileNumber"
            - hasProperty: "isPrimaryMobileNumber"
            - hasProperty: "location"
            - hasProperty: "name"
            - hasProperty: "personalBanker"
            - hasProperty: "phone"
            - hasProperty: "securityQuestions"
            - hasProperty: "segmentId"
            - hasProperty: "userId"
          capture:
            - json: "$.accountNumber"
              as: "accountNumber"
          log: "Posted locateProfile request"
      - think: 1
