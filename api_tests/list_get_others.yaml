# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=202607d963

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields: ["jetson_uuid"]
    skipHeader: true
scenarios:
  - name: "Negative tests for API Camera List"
    flow:
      - log: "Testing with invalid authorization credentials."
      - post:
          url: "/device/camera/list"
          headers:
            Authorization: "Invalid_Token {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
          expect:
            - statusCode: 401
            - result:
                description: { equals: "invalid authorization credentials"}
            - hasProperty: "description"
      - think: 5
      - log: "Testing with a missing required parameter."
      - post:
          url: "/device/camera/list"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid_missing: "{{ jetson_uuid }}"
          expect:
            - statusCode: 400
            - result:
                description: { equals: "bad input parameter"}
            - hasProperty: "description"
      - think: 5
      - log: "Testing with an invalid URL."
      - post:
          url: "/invalid/device/camera/list"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
          expect:
            - statusCode: 404
      - think: 5
