# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# RoostTestHash=202607d963

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      - "jetson_uuid"
    skipHeader: true
  environments:
    development:
      target: "http://localhost:3000"
    production:
      target: "http://api.example.com"

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Send request with invalid payload"
        post:
          url: "/device/camera/list"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid: "invalid_uuid"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
              - "error": "bad input parameter"
      - think: 1

      - log: "Send request without required parameters"
        post:
          url: "/device/camera/list"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
              - "error": "bad input parameter"
      - think: 1

      - log: "Send request to invalid URL"
        post:
          url: "/device/camera/invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Send request with invalid Authorization token"
        post:
          url: "/device/camera/list"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals:
              - "error": "Unauthorized"
      - think: 1
