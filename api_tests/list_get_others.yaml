# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c1f7bc6396

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      ["houseId"]
    skipHeader: true

scenarios:
  - name: "Test scenario for /device/list"
    flow:
      - log: "Sending request with invalid houseId"
        get:
          url: "/device/list?houseId=INVALID"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 5

      - log: "Sending request with missing houseId"
        get:
          url: "/device/list"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 5

      - log: "Sending request with invalid URL"
        get:
          url: "/device/list_invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 5

      - log: "Sending request with invalid authorization token"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
        think: 5
