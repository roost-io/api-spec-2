# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Non-success responses test scenario"
    flow:
      - log: "Sending a request with missing required query parameter"
        get:
          url: "/device/list"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - bodyContains:
                - "bad input parameter"
        # The server should respond with a 400 status code and an error message
      - think: 5
      - log: "Sending a request with invalid authorization token"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - bodyContains:
                - "Unauthorized"
        # The server should respond with a 401 status code and an error message
      - think: 5
      - log: "Sending a request to non-existing endpoint"
        get:
          url: "/nonexisting/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - bodyContains:
                - "Not Found"
        # The server should respond with a 404 status code and an error message
      - think: 5
