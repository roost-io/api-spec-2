# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Get device list from invalid houseId"
    flow:
      - log: "Sending request with invalid houseId"
        get:
          url: "/device/list?params={{ invalidHouseId }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals:
                description: "bad input parameter"
        think: 1

  - name: "Get device list without authorization"
    flow:
      - log: "Sending request without authorization"
        get:
          url: "/device/list?params={{ houseId }}"
          expect:
            - statusCode: 401
        think: 1

  - name: "Get device list with invalid URL"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/invalidURL?params={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
