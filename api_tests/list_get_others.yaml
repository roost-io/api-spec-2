# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields: 
      - "houseId"
    skipHeader: true 
  phases:
    - duration: 60
      arrivalRate: 20
      
scenarios:
  - name: "Non-success API Responses"
    flow:
      - log: "Request using a non-existing houseId"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'message'
            - hasProperty: 'errors'
            - equals: 
                message: "Invalid houseId"
     
      - think: 5

      - log: "Request with invalid Authorization token"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Invalid Token"
          expect:
            - statusCode: 401
            - hasProperty: 'message'
            - equals: 
                message: "Unauthorized"
                
      - think: 5

      - log: "Request to an invalid endpoint"
        get:
          url: "/device/lst?houseId={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'message'
            - equals: 
                message: "Invalid URL"
