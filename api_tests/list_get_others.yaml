# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Test device list API with failure scenarios"
    flow:
      - log: "Send a GET request without required params"
        get:
          url: "/device/list"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: 'error'
          - equals:
              error: 'bad input parameter'
          - hasHeader: 'content-type'
          - equals: { contentType: 'application/json' }
        # The API should respond with a 400 status code and a JSON containing error message.

      - think: 5
      
      - log: "Send a GET request with invalid authentication"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer invalid_token"
        expect:
          - statusCode: 401
        # The API should respond with a 401 status code indicating unauthorized access.
        
      - think: 5

      - log: "Send a POST request to the GET endpoint"
        post:
          url: "/device/list"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            houseId: "{{ houseId }}"
        expect:
          - statusCode: 405
          # The API should respond with a 405 status code indicating method not allowed.

      - think: 5

      - log: "Send a GET request to an invalid URL"
        get:
          url: "/device/invalid"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
          # The API should respond with a 404 status code indicating the requested resource could not be found.
