# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c1f7bc6396

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      ["params", "houseId"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Failure and Edge case testing for /device/list API"
    flow:
      - think: 20
      - post:
          url: "/device/list"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            params: "{{ params }}"
            houseId: "{{ houseId }}"
          capture:
            - json: "$.params"
              as: "params"
            - json: "$.houseId"
              as: "houseId"
          expect:
            - statusCode: 400
            - hasProperty: "params"
            - hasProperty: "houseId"
          log: "Testing with valid params and houseId but expecting a 400 status code"
      - think: 20
      - post:
          url: "/invalid/device/list"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            params: "{{ params }}"
            houseId: "{{ houseId }}"
          expect:
            - statusCode: 404
          log: "Testing with invalid URL"
      - think: 20
      - post:
          url: "/device/list"
          headers:
            Content-Type: "application/json"
            Authorization: "invalid_token"
          json:
            params: "{{ params }}"
            houseId: "{{ houseId }}"
          expect:
            - statusCode: 401
          log: "Testing with invalid authorization token"
