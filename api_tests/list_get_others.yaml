# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c1f7bc6396

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Test failure and edge cases for /device/list API endpoint"
    flow:
      - log: "Send GET request with invalid houseId"
        get:
          url: "/device/list?params={{ invalidHouseId }}"
          headers:
            Authorization: "Bearer invalidToken"
            Content-Type: "application/json"
        expect:
          - statusCode: 400
          - hasProperty: "error"
        # Here, we expect the server to return a 400 status code because the houseId is invalid

      - think: 1

      - log: "Send GET request without houseId"
        get:
          url: "/device/list?params="
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
        expect:
          - statusCode: 400
          - hasProperty: "error"
        # Here, we expect the server to return a 400 status code because the houseId is missing

      - think: 1

      - log: "Send GET request with invalid URL"
        get:
          url: "/invalidURL?params={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
        expect:
          - statusCode: 404
        # Here, we expect the server to return a 404 status code because the URL is invalid

      - think: 1

      - log: "Send GET request without authorization"
        get:
          url: "/device/list?params={{ houseId }}"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 401
        # Here, we expect the server to return a 401 status code because the authorization is missing
