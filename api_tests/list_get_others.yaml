# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c1f7bc6396

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "API test scenario for /device/list endpoint"
    flow:
      - log: "Test case: API request with invalid URL"
        get:
          url: "/invalid/url?params={{ houseId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1

      - log: "Test case: API request with invalid houseId parameter"
        get:
          url: "/device/list?params=invalid_houseId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 1

      - log: "Test case: API request without required houseId parameter"
        get:
          url: "/device/list"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 1

      - log: "Test case: API request with invalid authorization token"
        get:
          url: "/device/list?params={{ houseId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        think: 1
