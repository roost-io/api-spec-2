# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      - "invalidHouseId"
    skipHeader: true

scenarios:
  - name: "Test error handling of /device/list API endpoint"
    flow:
      # Test with an invalid houseId parameter
      - get:
          url: "/device/list?params={{ invalidHouseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with an invalid houseId to check 400 response"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - header: "Content-Type"
          comment: "Expecting a 400 bad input parameter response with an invalid houseId"

      # Introduce a think time
      - think: 2

      # Test with a missing houseId parameter
      - get:
          url: "/device/list?params="
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing without houseId to check 400 response"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - header: "Content-Type"
          comment: "Expecting a 400 bad input parameter response with missing houseId"

      # Introduce a think time
      - think: 2

      # Test with an invalid URL
      - get:
          url: "/invalid/device/list?params={{ invalidHouseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with an invalid URL to ensure proper error handling"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - header: "Content-Type"
          comment: "The server should return a 400 error for an invalid URL"

      # Introduce a think time
      - think: 2

      # Test the API with an invalid Authorization token
      - get:
          url: "/device/list?params={{ invalidHouseId }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with invalid authorization to check for access control"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - header: "Content-Type"
          comment: "Expecting a 400 response due to invalid authorization token"

