# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c1f7bc6396

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "API failure test cases"
    flow:
      - log: "Send request with missing required query parameter"
        get:
          url: "/device/list"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: 'error'
          - equals: 
              - "{{ response.error }}"
              - "bad input parameter"
        # Expected outcome: The API should return a 400 status code and an error message indicating the missing required parameter.

      - think: 1

      - log: "Send request with invalid houseId query parameter"
        get:
          url: "/device/list?params={\"houseId\":\"invalidHouseId\"}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: 'error'
          - equals: 
              - "{{ response.error }}"
              - "bad input parameter"
        # Expected outcome: The API should return a 400 status code and an error message indicating the invalid parameter.

      - think: 1

      - log: "Send request with invalid URL"
        get:
          url: "/device/invalidUrl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        # Expected outcome: The API should return a 404 status code indicating the invalid URL.

      - think: 1

      - log: "Send request with invalid Authorization token"
        get:
          url: "/device/list?params={\"houseId\":\"{{ houseId }}\"}"
          headers:
            Authorization: "InvalidAuthToken"
        expect:
          - statusCode: 401
        # Expected outcome: The API should return a 401 status code indicating the invalid Authorization token.
