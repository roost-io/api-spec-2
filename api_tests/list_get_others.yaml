# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Failure and Edge Cases for /device/list endpoint"
    flow:
      - log: "Sending a request with missing 'houseId' parameter"
        get:
          url: "/device/list"
          # Expected to fail due to missing required 'houseId' parameter
          expect:
            - statusCode: 400
            - hasProperty: 'description'

      - think: 1

      - log: "Sending a request with invalid 'houseId' parameter format"
        get:
          url: "/device/list?houseId=INVALID_FORMAT"
          headers:
            Content-Type: "application/json"
          # Expected to fail due to invalid 'houseId' format
          expect:
            - statusCode: 400
            - hasProperty: 'description'

      - think: 1

      - log: "Sending a request with an incorrect URL"
        get:
          url: "/device/lis" # Typo in endpoint
          # Expected to fail due to incorrect URL
          expect:
            - statusCode: 404

      - think: 1

      - log: "Sending a request with invalid authorization token"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer invalid_token"
          # Expected to fail due to invalid authorization
          expect:
            - statusCode: 401
            - hasProperty: 'description'

      - think: 1

      - log: "Sending a request with an expired authorization token"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer expired_token"
          # Expected to fail due to expired authorization
          expect:
            - statusCode: 403
            - hasProperty: 'description'
