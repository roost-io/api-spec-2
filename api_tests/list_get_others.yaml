# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payloads:
    - path: "list_get_others.csv"
      fields: ["houseId"]
      skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Test failure cases for /device/list API"
    flow:
      - log: "Sending a valid request, expecting a 400 response because of bad input parameter"
        post:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["error.message", "bad input parameter"]
        think: 2

      - log: "Sending a request with invalid auth token, expecting a 401 response"
        post:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
        think: 2

      - log: "Sending a request to an incorrect URL, expecting a 404 response"
        post:
          url: "/incorrect/url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 2

      - log: "Sending a request without required query params, expecting a 400 response"
        post:
          url: "/device/list"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 2
