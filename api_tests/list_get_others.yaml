# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=202607d963

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payloads:
  - name: "params"
    path: "list_get_others.csv"
    skipHeader: true
    fields:
    - "jetson_uuid"
scenarios:
- name: "Test Non-Success API Responses"
  flow:
  - log: "This request demonstrates a bad input parameter with an empty string as a jetson_uuid"
    post:
      url: "/device/camera/list"
      json:
        jetson_uuid: ""
      headers:
        Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      expect:
      - statusCode: 400
  - think: 1
  - log: "This request demonstrates authentication issues with an invalid token"
    post:
      url: "/device/camera/list"
      json:
        jetson_uuid: "TestUUID"
      headers:
        Authorization: "InvalidToken"
      expect:
      - statusCode: 401     
  - think: 1
  - log: "This request demonstrates the scenario where the requested URL does not exist"
    post:
      url: "/invalidUrl"
      json:
        jetson_uuid: "TestUUID"
      headers:
        Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      expect:
      - statusCode: 404
