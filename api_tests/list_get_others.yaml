# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  override: 
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Attempting request with invalid payload"
        post:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            Authorization: "InvalidAuthToken"
          json:
            invalidField: "invalidValue"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            # Expecting a 400 status code and a message property in the response body

      - think: 1

      - log: "Attempting request with invalid URL"
        post:
          url: "/invalid/url?houseId={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            # Expecting a 404 status code

      - think: 1

      - log: "Attempting request without required parameters"
        post:
          url: "/device/list"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            # Expecting a 400 status code
