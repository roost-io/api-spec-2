# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Test device list API"
    flow:
      - log: "Testing invalid URL"
        get:
          url: "/device/lis"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 2

      - log: "Testing without authorization token"
        get:
          url: "/device/list?params={{ houseId }}"
          expect:
            - statusCode: 401
        think: 2

      - log: "Testing with invalid houseId"
        get:
          url: "/device/list?params=invalidHouseId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 2

      - log: "Testing with missing query parameter"
        get:
          url: "/device/list"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 2
