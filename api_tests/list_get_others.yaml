# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=202607d963

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    skipHeader: true
    fields:
      - "jetson_uuid"

scenarios:
  - name: "Failure Cases for /device/camera/list API endpoint"
    flow:
      - log: "Sending a request without necessary headers"
        get:
          url: "/device/camera/list"
          headers:
            Authorization: "Bearer incorrect_token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals:
                message: "Unauthorized"

      - think: 10

      - log: "Sending a request with wrong parameters"
        get:
          url: "/device/camera/list?jetson_uuid={{jetson_uuid}}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals:
                message: "bad input parameter"

      - think: 10

      - log: "Sending a request to an incorrect URL"
        get:
          url: "/device/camera/list1"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals:
                message: "Not found"
