# Test generated by RoostGPT for test qlue-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    fields:
      - username
      - password
    path: "list_get_others.csv"
    skipHeader: true
scenarios:
  - name: "Failed API Calls Scenario"
    flow:
      - log: "Attempt a request with wrong authentication token"
        post:
          headers:
            Authorization: "WrongToken"
          url: "/device/list?houseId=abcd123"
          expect:
            - statusCode: 401
            - hasProperty: "errors"
        # Invalid authentication token should return 401 Unauthorized and contain errors property in the response

      - think: 5

      - log: "Attempt a request without required query parameter"
        get:
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          url: "/device/list"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
        # Skipping required houseId query should return 400 bad input parameter and contain errors property in the response

      - think: 5

      - log: "Attempt a request with incorrect URL"
        get:
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          url: "/device/wrong"
          expect:
            - statusCode: 404
        # Incorrect URL should return 404 - Not found
