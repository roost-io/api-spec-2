# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Get device list failure scenarios"
    flow:
      - log: "Test scenario: missing houseId in query params"
        get:
          url: "/device/list?params"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - bodyContains:
                - "bad input parameter"
      - think: 2

      - log: "Test scenario: invalid houseId in query params"
        get:
          url: "/device/list?params={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - bodyContains:
                - "bad input parameter"
      - think: 2

      - log: "Test scenario: invalid url"
        get:
          url: "/device/list_invalid?params={{ houseId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 2

      - log: "Test scenario: unauthorized request"
        get:
          url: "/device/list?params={{ houseId }}"
          headers:
            Authorization: "INVALID_TOKEN"
          expect:
            - statusCode: 401
      - think: 2
