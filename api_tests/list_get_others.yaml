# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4


# RoostTestHash=fe16559128

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "list_get_others.csv"
    fields:
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Testing API /device/list"
    flow:
      - log: "Testing with empty houseId"
        get:
          url: "/device/list?houseId="
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.statusCode"
            as: "responseStatus"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
        think: 1

      - log: "Testing with invalid houseId"
        get:
          url: "/device/list?houseId=invalid"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.statusCode"
            as: "responseStatus"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
        think: 1

      - log: "Testing with invalid authorization"
        get:
          url: "/device/list?houseId={{ houseId }}"
          headers:
            authorization: "invalid"
          capture:
            json: "$.statusCode"
            as: "responseStatus"
          expect:
            - statusCode: 401
            - hasProperty: "errors"
        think: 1

      - log: "Testing with invalid URL"
        get:
          url: "/invalid/list?houseId={{ houseId }}"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.statusCode"
            as: "responseStatus"
          expect:
            - statusCode: 404
            - hasProperty: "errors"
