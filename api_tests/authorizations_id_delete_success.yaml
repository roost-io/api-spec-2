# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_success.csv"
    fields:
      - id
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Remove Authorized Account"
    flow:
      - log: "Starting the test scenario for removing an authorized account"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 5
