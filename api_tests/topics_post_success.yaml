# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /kafka/v3/clusters/{cluster_id}/topics_post for http method type POST in artillery framework

# RoostTestHash=783e9d2ab5


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "topics_post_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "partitions_count"
      - "replication_factor"
      - "configs"
      - "validate_only"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Create Kafka Topic Scenario"
    flow:
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            topic_name: "{{ topic_name }}"
            partitions_count: "{{ partitions_count }}"
            replication_factor: "{{ replication_factor }}"
            configs: "{{ configs }}"
            validate_only: "{{ validate_only }}"
          log: "Creating a Kafka topic with name {{ topic_name }}"
          expect:
            - statusCode: 201
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "cluster_id"
            - hasProperty: "topic_name"
            - hasProperty: "is_internal"
            - hasProperty: "replication_factor"
            - hasProperty: "partitions_count"
            - hasProperty: "partitions"
            - hasProperty: "configs"
            - hasProperty: "partition_reassignments"
            - hasHeader: "content-type"
      - think: 1

      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            topic_name: "{{ topic_name }}"
            partitions_count: "{{ partitions_count }}"
            replication_factor: "{{ replication_factor }}"
            validate_only: true
          log: "Dry run creating a Kafka topic with name {{ topic_name }}"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "cluster_id"
            - hasProperty: "topic_name"
            - hasProperty: "is_internal"
            - hasProperty: "replication_factor"
            - hasProperty: "partitions_count"
            - hasProperty: "partitions"
            - hasProperty: "configs"
            - hasProperty: "partition_reassignments"
            - hasHeader: "content-type"
      - think: 1
