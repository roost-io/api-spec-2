# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /kafka/v3/clusters/{cluster_id}/topics_post for http method type POST in artillery framework

# RoostTestHash=783e9d2ab5


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "topics_post_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "partitions_count"
      - "replication_factor"
      - "config_name"
      - "config_value"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Create Kafka Topic Scenario"
    flow:
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            topic_name: "{{ topic_name }}"
            partitions_count: "{{ partitions_count }}"
            replication_factor: "{{ replication_factor }}"
            configs:
              - name: "{{ config_name }}"
                value: "{{ config_value }}"
          expect:
            - statusCode: 201
          log: "Creating a new Kafka topic with valid configuration."
          think: 1
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            topic_name: "{{ topic_name }}"
            partitions_count: "{{ partitions_count }}"
            replication_factor: "{{ replication_factor }}"
            validate_only: true
          expect:
            - statusCode: 200
          log: "Validating Kafka topic creation with dry-run mode."
          think: 1
