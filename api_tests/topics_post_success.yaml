# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# Test generated for /kafka/v3/clusters/{cluster_id}/topics_post for http method type POST in artillery framework
# RoostTestHash=fa240a7735

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "topics_post_success.csv"
    fields:
      - "cluster_id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Post request to /kafka/v3/clusters/{cluster_id}/topics"
    flow:
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 201
              hasProperty: ['id', 'status', 'code', 'title', 'detail', 'source', 'source.pointer', 'source.parameter', 'error_code', 'message']
          log: "Post request to /kafka/v3/clusters/{cluster_id}/topics was successful"
        think: 1
