# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=4ee5afee1b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "partitions_get_others.csv"
    fields:
      - "cluster_id"
      - "topic_name"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "List Partitions Negative Test Cases"
    flow:
      - log: "Testing with invalid URL"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/invalidEndpoint"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Testing with invalid Authorization token"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Testing with missing Authorization token"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Testing with non-existing topic name"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/non_existing_topic/partitions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Testing with non-existing cluster ID"
      - get:
          url: "/kafka/v3/clusters/non_existing_cluster/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

      - log: "Testing with rate limit exceeded"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 429
      - think: 1

      - log: "Testing with server-side error"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "error_code"
            - hasProperty: "message"
