# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=7a4b4c4432

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  processor: "./my_processor.js"
  payload:
    path: "partitions_get_others.csv"
    fields:
      - "cluster_id"
      - "topic_name"
  plugins:
    expect: {}
scenarios:
  - name: "Test /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions API endpoint"
    flow:
      - log: "Sending invalid request with invalid cluster_id"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "Bearer {{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
      - log: "Sending invalid request with missing topic_name"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics//partitions"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
      - log: "Sending request with invalid authorization token"
        get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "code"
            - hasProperty: "title"
            - hasProperty: "detail"
            - hasProperty: "source"
            - hasProperty: "source.pointer"
            - hasProperty: "source.parameter"
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
      - log: "Sending request to invalid URL"
        get:
          url: "/invalid/path/{{ cluster_id }}/topics/{{ topic_name }}/partitions"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
