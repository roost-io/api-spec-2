# Test generated by RoostGPT for test qlue-single-api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /GetTransactions_post for http method type POST in artillery framework

# RoostTestHash=207beafabe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetTransactions_post_success.csv"
    fields:
      - "customerId"
      - "accountNumber"
      - "numOfTransactions"
      - "status"
      - "startDate"
      - "endDate"
      - "sort"
      - "transactionAmount"
      - "category"
      - "debitOrCredit"
    skipHeader: true
scenarios:
  - name: "GetTransactions API Test"
    flow:
      - post:
          url: "/GetTransactions"
          headers:
            koreUserId: "{{$processEnvironment.KORE_USER_ID}}"
            botId: "{{$processEnvironment.BOT_ID}}"
            accountId: "{{$processEnvironment.ACCOUNT_ID}}"
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
            "X-Auth-token": "{{$processEnvironment.X_AUTH_TOKEN}}"
            "X-CORRELATION-ID": "{{$processEnvironment.X_CORRELATION_ID}}"
          json:
            customerId: "{{ customerId }}"
            accountNumber: "{{ accountNumber }}"
            numOfTransactions: "{{ numOfTransactions }}"
            status: "{{ status }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            sort: "{{ sort }}"
            transactionAmount: "{{ transactionAmount }}"
            category: "{{ category }}"
            debitOrCredit: "{{ debitOrCredit }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "transactionId"
            - hasProperty: "accountNumber"
            - hasProperty: "timestamp"
            - hasProperty: "amount"
            - hasProperty: "currency"
            - hasProperty: "debitOrCredit"
            - hasProperty: "description"
            - hasProperty: "paymentMode"
            - hasProperty: "DBANames"
            - hasProperty: "status"
            - hasProperty: "transactionType"
            - hasProperty: "MCCCode"
            - hasProperty: "name"
            - hasProperty: "address"
            - hasProperty: "street"
            - hasProperty: "city"
            - hasProperty: "state"
            - hasProperty: "zip"
            - hasProperty: "country"
            - hasProperty: "phoneNumber"
          think: 1
