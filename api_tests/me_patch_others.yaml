# Test generated by RoostGPT for test credential-artillery-test-updated using AI Type Open AI and AI Model gpt-4


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  processor: "./my-functions.js"

scenarios:
  - name: Non-success API requests scenario
    flow:
      # Test case with invalid token
      - post:
          url: "/me"
          headers:
            Token: "InvalidToken"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 401
          think: 1

      # Test case with missing required field 'name'
      - post:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
          think: 1

      # Test case with invalid URL
      - post:
          url: "/invalid-url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 404
          think: 1

      # Test case with incorrect image data format
      - post:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "invalidImageData"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
          think: 1
