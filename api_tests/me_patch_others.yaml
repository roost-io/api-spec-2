# Test generated by RoostGPT for test credential-artillery-test-updated using AI Type Open AI and AI Model gpt-4


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10
scenarios:
  - name: "Test non-successful API responses"
    flow:
      - log: "Sending PATCH request with invalid payload"
        patch:
          url: "/me"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "invalid_name"
            image: "invalid_image"
            settings: "invalid_settings"
          expect:
            - statusCode: 400
        think: 1
      - log: "Sending PATCH request without authorization token"
        patch:
          url: "/me"
          headers:
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 401
        think: 1
      - log: "Sending PATCH request to non-existing endpoint"
        patch:
          url: "/non_existing_endpoint"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 404
        think: 1
