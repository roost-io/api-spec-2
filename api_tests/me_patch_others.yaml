# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "./me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      - log: "Sending a PATCH request with invalid payload"
        patch:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400 # expecting a bad request response
        think: 1

      - log: "Sending a PATCH request with missing authorization token"
        patch:
          url: "/me"
          headers:
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 401 # expecting an unauthorized response
        think: 1

      - log: "Sending a PATCH request to an invalid URL"
        patch:
          url: "/invalid_url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 404 # expecting a not found response
        think: 1
