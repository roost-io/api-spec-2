# Test generated by RoostGPT for test credential-artillery-test using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=6dd436357d

---
config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      name: Token
      in: header
scenarios:
  - scenario: Failure Cases
    flow:
      - get:
          url: /me
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 401
            contentType: application/json
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
            hasProperty: error
            hasProperty: description
      - post:
          url: /me
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            name: "John Doe"
          expect:
            statusCode: 400
            contentType: application/json
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
            hasProperty: error
            hasProperty: description
      - post:
          url: /me
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            name: "John Doe"
            image: "invalid-base64-string"
          expect:
            statusCode: 400
            contentType: application/json
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
            hasProperty: error
            hasProperty: description
      - post:
          url: /me
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            name: "John Doe"
            image: "data:image/jpeg;base64,invalid-base64-string"
          expect:
            statusCode: 400
            contentType: application/json
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
            hasProperty: error
            hasProperty: description
      - post:
          url: /me
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            name: "John Doe"
            settings:
              invalid-setting: "invalid-value"
          expect:
            statusCode: 400
            contentType: application/json
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
            hasProperty: error
            hasProperty: description
      - get:
          url: /invalid-url
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 404
            contentType: application/json
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
            hasProperty: error
            hasProperty: description
      - think: 500
