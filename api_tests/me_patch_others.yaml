# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_patch_others.csv"
    skipHeader: true
    fields:
      - "name"
      - "image"
      - "settings"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      - post:
          url: "/me"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
          log: "Testing invalid payload with missing required fields"
          think: 1

      - post:
          url: "/me"
          headers:
            Content-Type: "application/json"
          json:
            name: "InvalidName"
            image: "InvalidImage"
            settings: {}
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
          log: "Testing without authentication token"
          think: 1

      - post:
          url: "/not_existing_endpoint"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 404
          log: "Testing invalid URL"
          think: 1

      - post:
          url: "/me"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: 12345 # Invalid data type for 'name'
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
            - jmespath: "error == 'validation/name-type'"
          log: "Testing invalid data type for 'name'"
          think: 1

      - post:
          url: "/me"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "not_base64" # Invalid format for 'image'
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
            - jmespath: "error == 'validation/image-format'"
          log: "Testing invalid format for 'image'"
          think: 1
