# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API Failure Scenarios"
    flow:
      - log: "Testing with invalid authentication token"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer invalid_token"
          json:
            name: "John Doe"
            image: "data:image/jpeg;base64,invalid_base64"
            settings: {}
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 2

      - log: "Testing with invalid payload (missing required fields)"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            image: "data:image/jpeg;base64,invalid_base64"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 2

      - log: "Testing with invalid payload (invalid image format)"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "John Doe"
            image: "not_a_base64_string"
            settings: {}
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "schema_field"
            - hasProperty: "value"
      - think: 2

      - log: "Testing with invalid URL"
      - post:
          url: "/invalid_url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "John Doe"
            image: "data:image/jpeg;base64,valid_base64"
            settings: {}
          expect:
            - statusCode: 404
      - think: 2
