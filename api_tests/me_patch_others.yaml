# Test generated by RoostGPT for test credential-artillery-test-withlogs using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    - path: "me_patch_others.csv"
      fields:
        - "name"
        - "image"
        - "settings"
      skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-successful API responses"
    flow:
      - log: "Testing invalid payload with missing 'name' field."
      - post:
          url: "/me"
          json:
            image: "data:image/jpeg;base64,invalidbase64"
            settings: {}
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1
      - log: "Testing invalid authentication."
      - post:
          url: "/me"
          json:
            name: "John Doe"
            image: "data:image/jpeg;base64,validbase64=="
            settings: {}
          headers:
            authorization: "Bearer invalidtoken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1
      - log: "Testing invalid URL."
      - post:
          url: "/invalid-url"
          json:
            name: "John Doe"
            image: "data:image/jpeg;base64,validbase64=="
            settings: {}
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
