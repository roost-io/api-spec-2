# Test generated by RoostGPT for test qlue-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Test failure and edge cases for /me endpoint"
    flow:
      # Invalid Authorization Header
      - log: "Sending request with invalid authorization header"
      - post:
          url: "/me"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasHeader: 'api-version'
      - think: 2

      # Missing required fields
      - log: "Sending request with missing required fields"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: ""
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasHeader: 'api-version'
      - think: 2

      # Invalid URL
      - log: "Sending request to an invalid URL"
      - post:
          url: "/invalid-url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 404
      - think: 2

      # Invalid image format
      - log: "Sending request with invalid image format"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "not_base64_data"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasHeader: 'api-version'
      - think: 2

      # Invalid settings object
      - log: "Sending request with invalid settings object"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "not_an_object"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasHeader: 'api-version'
