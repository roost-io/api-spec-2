# Test generated by RoostGPT for test credential-artillery-test using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      plugins:
        expect: {}
  payload:
    path: "authorizations_get_others.csv"
    fields:
      - "username"
      - "password"
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending invalid GET request to /authorizations"
        get:
          url: "/authorizations"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
        think: 1
      - log: "Sending GET request to /authorizations with invalid token"
        get:
          url: "/authorizations"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 401
        think: 1
      - log: "Sending GET request to invalid URL"
        get:
          url: "/invalid_url"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1
